# 契约 · 配额模式与餐次分包（T-102 冻结版）

> 归属：Sprint「对齐『好人松松』减脂方法论」T-102 · 只产出契约，不含业务代码。
> 上游依据：[减脂方法论改造.md](file:///d:/02工作/健身助手/减脂方法论改造.md)（第一、二、四、五、六章）、官方 Excel（`【可任意分享】健身Excel超级套表…26年9月最新版.xlsx`，第 1–8 表 + 第 17 表）、[CALC-AUDIT.md](file:///d:/02工作/健身助手/CALC-AUDIT.md)。
> 下游任务：T-103（后端 settings）、T-104（前端配额引擎）、T-106（后端分包 + 迁移）、T-107（mealTargets）、T-108、T-109。
> 状态：**冻结**。本文件一经冻结，T-103 / T-104 / T-106 三方按此并行实现，不再互相等待。
>
> **行号锚点说明**：本文件中的 `file:///…` 行号锚点是 T-102 冻结时刻的快照。`utils.js` 在 T-105 ~ T-118 间被大量插改（新增配额派、分餐引擎与 `perMealIntake` / `potDaysOf`），行号已整体下移。**定位一律以函数 / 变量名为准**，行号仅作历史标记，不逐个重锚。

---

## 0. 冻结范围与前置口径

### 0.1 本契约冻结的四件事

| # | 契约 | 落地文件（实现方） |
|---|---|---|
| ① | `settings` 新增键（键名 / 类型 / 枚举 / 默认 / 校验） | [backend/src/routes/settings.js](file:///d:/02工作/健身助手/backend/src/routes/settings.js)、[backend/src/seed.js](file:///d:/02工作/健身助手/backend/src/seed.js)、[frontend/src/constants.js](file:///d:/02工作/健身助手/frontend/src/constants.js) |
| ② | 配额表常量结构（阶段 × 性别 × 日类型 + BMI 两档修正 + 初期/末期） | 新增前端常量模块 + `calcQuotaProfile()`（T-104） |
| ③ | `mealTargets()` 输入输出结构（日类型 + 训练时间点 + 全天配额 → 各餐目标） | [frontend/src/utils.js](file:///d:/02工作/健身助手/frontend/src/utils.js)（T-107） |
| ④ | `recipes` / `inventory` / `day_logs` 的餐次分包数据形状与旧数据迁移 | [backend/src/db.js](file:///d:/02工作/健身助手/backend/src/db.js)、[backend/src/routes/recipes.js](file:///d:/02工作/健身助手/backend/src/routes/recipes.js)、[backend/src/routes/inventory.js](file:///d:/02工作/健身助手/backend/src/routes/inventory.js)、[backend/src/routes/day-logs.js](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js)（T-106 / T-109） |

### 0.2 已冻结的三处口径决定（不自选，采自 `.trae/memory/current-sprint.md` 文末）

| # | 分歧点 | 冻结口径 |
|---|---|---|
| 1 | 休息日碳水 | 取**休息日专页绝对值**：增肌 2.5–3 g/kg、减脂 1.5–2 g/kg。**不采用**配额表脚注「训练日 −0.5 g」 |
| 2 | 减脂期其他餐碳水比例 | 取**逐字稿口述**：`早饭 2 : 练前 2 : 练后 4 : 其他 2`，随碳水递减演进为 `3:2:5`（其他餐趋 0）。**不采用**页面上含义不明的 `2242 → 3052` |
| 3 | GI 清单 | 本 Sprint **不涉及**，配额表常量中不包含任何 GI 字段 |

### 0.3 术语与枚举（全契约共用）

```js
/* 餐次槽位（mealSlot）—— 日内进食场合的全集，共 6 个 */
export const MEAL_SLOTS = ['breakfast', 'lunch', 'pre', 'post', 'dinner', 'snack'];

/* 可分包的餐次（packSlot）—— 备餐锅能覆盖的「正餐段」餐次，共 5 个。
 * snack（零食/夜宵）不进分包：其 10% 碳水是机动抵扣额度（牛奶/蔬菜/调料里未计入的碳水），
 * 在 day_logs 里由 late 字段承接，见 §4.4 */
export const PACK_SLOTS = ['breakfast', 'lunch', 'pre', 'post', 'dinner'];
```

| 槽位 | 中文 | 说明 |
|---|---|---|
| `breakfast` | 早饭 | 训练日可能身兼「早饭 + 练前餐」两角 |
| `lunch` | 午饭 | 训练日可能身兼「午饭 + 练前餐」或「午饭 + 练后餐」 |
| `pre` | 练前餐 | 独立餐次，仅 `before_lunch` / `before_dinner` / `after_lunch` / `after_dinner` 4 种时间点存在 |
| `post` | 练后餐 | 独立餐次；`breakfast_late` 中由午饭承担、`before_lunch` 中由午饭承担 |
| `dinner` | 晚饭 | 训练日可能身兼「晚饭 + 练前餐」或「晚饭 + 练后餐」 |
| `snack` | 零食/夜宵 | 休息日/无训练固定 10% 碳水；训练日不进 100% 分配（见 §2.3 推导） |

---

## 一、契约 ①：`settings` 新增键

### 1.1 现状约定（实现前必须遵守）

- `settings` 是 key-value 表：`CREATE TABLE settings (key TEXT PRIMARY KEY, value TEXT NOT NULL)`，`value` 是 JSON 字符串 → [backend/src/db.js#L44-L47](file:///d:/02工作/健身助手/backend/src/db.js#L44-L47)
- 读取走 `readAllSettings()`，整体反序列化成对象 → [backend/src/helpers.js#L49-L54](file:///d:/02工作/健身助手/backend/src/helpers.js#L49-L54)
- 写入走 `PUT /api/settings`，**部分更新**（只覆盖请求里出现的键），白名单外的键一律 400 → [backend/src/routes/settings.js#L11-L57](file:///d:/02工作/健身助手/backend/src/routes/settings.js#L11-L57)，未知键报错在 [#L52-L54](file:///d:/02工作/健身助手/backend/src/routes/settings.js#L52-L54)
- 默认值必须**三处同步**，缺一处就会出现「前端有默认、后端没有」的漂移：
  1. [backend/src/seed.js#L53-L58](file:///d:/02工作/健身助手/backend/src/seed.js#L53-L58) `DEFAULT_SETTINGS`
  2. [backend/src/db.js#L114-L129](file:///d:/02工作/健身助手/backend/src/db.js#L114-L129) `ensureSettingsKeys()`（老库幂等补键，`INSERT OR IGNORE` 不覆盖用户值）
  3. [frontend/src/constants.js#L71-L76](file:///d:/02工作/健身助手/frontend/src/constants.js#L71-L76) `SETTINGS_FALLBACK`

### 1.2 新增键定义表

| 键名 | 类型 | 取值枚举 / 范围 | 默认值 | 语义 | 后端校验 |
|---|---|---|---|---|---|
| `calcMode` | string | `'tdee'` \| `'quota'` | `'tdee'` | 计算模式。`tdee` = 现有「BMR→TDEE→减 gap→反推宏量」；`quota` = 配额派「查 g/kg → 热量是结果」 | **需要**。非枚举值 → 400 `calcMode 只允许 tdee/quota` |
| `phase` | string | `'gain'` \| `'cut'` | `'cut'` | 目标阶段：增肌期 / 减脂期。决定查配额表的哪一半 | **需要**。非枚举值 → 400 |
| `dayType` | string | `'train'` \| `'rest'` \| `'none'` | `'none'` | 日类型。`train` 训练日；`rest` 休息日；`none` 无训练（天天休息日，居家不运动方案） | **需要**。非枚举值 → 400 |
| `trainSlot` | string | 7 值枚举，见 §1.3 | `'before_dinner'` | 力训时间点，决定哪个餐次扮演练前餐 / 练后餐 | **需要**。非枚举值 → 400。`dayType !== 'train'` 时**不报错**，仅被引擎忽略（值保留，切回训练日即恢复） |
| `carbStage` | string | `'early'` \| `'late'` | `'early'` | 减脂碳水递减档位：初期 / 末期。决定碳水区间取 `[2.5,3]` 还是 `[2,2.5]` | **需要**。非枚举值 → 400。`phase === 'gain'` 时被忽略 |
| `carbPer` | number \| null | `1.0 – 6.0`，1 位小数 | `null` | 碳水配额**覆盖值**（g/kg）。`null` = 按配额表 + BMI 修正取值。用户 ±0.5 档位调整的载体 | **需要**。`null` 合法；非数字或超出 1.0–6.0 → 400 |
| `fatPer` | number \| null | `0.3 – 1.5`，1 位小数 | `null` | 脂肪配额**覆盖值**（g/kg）。`null` = 按配额表 + BMI 修正取值 | **需要**。`null` 合法；非数字或超出 0.3–1.5 → 400 |
| `proteinPer` | number | `1.2 – 2.2`，1 位小数 | `1.5` | 蛋白配额 g/kg。**该键已存在**（见 §6.1 差异说明），配额模式直接复用，不新增 | **需要补齐**。现值只校验 `Number.isFinite`（[settings.js#L28-L30](file:///d:/02工作/健身助手/backend/src/routes/settings.js#L28-L30)），需补 1.2–2.2 范围校验；种子值 1.5 与前端钳制区间 [1.2, 2.2]（[SettingsView.vue#L33](file:///d:/02工作/健身助手/frontend/src/views/SettingsView.vue#L33)）均在范围内，不回滚存量 |

### 1.3 `trainSlot` 七值枚举（与官方 Excel 第 1–7 张减脂表一一对应）

| 枚举值 | 标签 | 对应官方表页 | 官方餐序（表内「力训时间」单元格原文） |
|---|---|---|---|
| `breakfast_early` | 早饭后练（早起版） | 1减脂-早饭后练（早起版） | ①早饭=练前餐 ②练后餐 ③午饭=其他餐 ④晚饭=其他餐 |
| `breakfast_late` | 早饭后练（晚起版） | 2减脂-早饭后练（晚起版） | ①早饭=练前餐 ②午饭=练后餐 ③晚饭=其他餐 |
| `before_lunch` | 午饭前练 | 3减脂-午饭前练 | ①早饭 ②练前餐 ③午饭=练后餐 ④晚饭=其他餐 |
| `after_lunch` | 午饭后练 | 4减脂-午饭后练 | ①早饭 ②午饭=练前餐 ③练后餐 ④晚饭=其他餐 |
| `before_dinner` | 晚饭前练 | 5减脂-晚饭前练 | ①早饭 ②午饭=其他餐 ③练前餐 ④晚饭=练后餐 |
| `after_dinner` | 晚饭后练 | 6减脂-晚饭后练 | ①早饭 ②午饭=其他餐 ③晚饭=练前餐 ④练后餐 |
| `night` | 夜里练 | 7减脂-夜里练 | ①早饭 ②午饭=其他餐 ③晚饭=其他餐 ④练后餐 |

> 上表逐字抄自 Excel 第 1–7 张表 `B12/C12` 单元格（「力训时间」行），与第 17 表《减脂-问答汇总》第 15 问的七条餐序完全一致。
> 第 7 种在第 17 问里写作【晚饭后更晚练】，在表页名里写作「夜里练」——**本契约以表页名为准**，枚举值取 `night`，标签取「夜里练」。
> 第 17 问对第 7 种的补充说明：「各餐可以比较均摊，练后餐稍大一点即可，否则白天会太饿」——已体现在 §2.3 的比例推导里。

### 1.4 取值优先级（冻结，不得改动）

对每一项配额，读取顺序如下（**逐项独立判断**）：

```
BMI 修正生效（phase==='cut' 且 BMI 命中 >28 或 >32）
  ├── 碳水：settings.carbPer ?? 修正值        // 碳水是唯一日常调整旋钮，允许覆盖
  ├── 蛋白：修正值                            // 强制，忽略 proteinPer
  └── 脂肪：修正值                            // 强制，忽略 fatPer
BMI 修正不生效
  ├── 碳水：settings.carbPer ?? 配额表区间下界
  ├── 蛋白：settings.proteinPer ?? 配额表区间下界
  └── 脂肪：settings.fatPer ?? 配额表区间下界
```

**为什么 BMI 修正要压过设置值**：原视频口径是「以碳水的增减作为热量变量，蛋白质和脂肪可以略加但不能再往下调」（[减脂方法论改造.md#L61](file:///d:/02工作/健身助手/减脂方法论改造.md#L61)）。BMI>28 的降配属于**安全约束**（肥胖人群先减脂、降低起始配额），不能被 TDEE 模式留下的 `proteinPer=1.5` 存量值顶掉；而碳水留出覆盖口，是因为 ±0.5 档位调整全部作用在碳水一项上。T-104 验收样例「BMI 29 减脂期男 → 起始碳水 2.5、蛋白 1.2、脂肪 0.6 g/kg」正是靠这条规则成立。

### 1.5 模式切换时的字段处置

| 键 | `calcMode='tdee'` | `calcMode='quota'` |
|---|---|---|
| `act` / `gap` / `manualTdee` / `fatRatio` | 生效（现状不变） | **保留在库、计算时忽略**；不删除、不清零，切回 TDEE 模式后行为与切换前完全一致 |
| `carbPer` / `fatPer` / `carbStage` / `phase` / `dayType` / `trainSlot` | 保留在库、计算时忽略 | 生效 |
| `proteinPer` | 生效（现状：`p = s.weight * s.proteinPer`，[utils.js#L30](file:///d:/02工作/健身助手/frontend/src/utils.js#L30)） | 生效（同语义，仅体重口径改为入参 `bodyWeight`） |

### 1.6 请求 / 响应示例

**切换模式并设日类型（部分更新）**

```http
PUT /api/settings
Content-Type: application/json
```

```json
{
  "calcMode": "quota",
  "phase": "cut",
  "dayType": "train",
  "trainSlot": "before_dinner",
  "carbStage": "early",
  "carbPer": 2.5,
  "fatPer": 0.8
}
```

**响应 200（返回完整设置对象，数值已反序列化）**

```json
{
  "code": 0,
  "data": {
    "weight": 90, "height": 175, "age": 30, "sex": "m",
    "act": 1.375, "gap": 750, "proteinPer": 1.5, "fatRatio": 23,
    "manualTdee": null, "addonsOn": true, "current_recipe_id": 1,
    "targetWeight": 80, "weeklyRate": 0.5, "weightTrack": false,
    "calcMode": "quota", "phase": "cut", "dayType": "train",
    "trainSlot": "before_dinner", "carbStage": "early",
    "carbPer": 2.5, "fatPer": 0.8
  }
}
```

**响应 400（非法枚举）**

```json
{ "code": 1, "message": "trainSlot 只允许 breakfast_early/breakfast_late/before_lunch/after_lunch/before_dinner/after_dinner/night" }
```

**响应 400（未知键，行为保持不变）**

```json
{ "code": 1, "message": "未知设置项: carbRatio" }
```

### 1.7 默认值增补（三处同步写同一份）

```js
/* 追加到 DEFAULT_SETTINGS / ensureSettingsKeys.defaults / SETTINGS_FALLBACK */
calcMode: 'tdee',        // 默认沿用既有 TDEE 模式，切换是用户的显式动作
phase: 'cut',            // 与 targetWeight(80) / weeklyRate(0.5) 的既有减脂语义一致
dayType: 'none',         // 用户当前执行的就是「居家不运动」方案（改造 md 第二章）
trainSlot: 'before_dinner', // dayType!=='train' 时惰性无效
carbStage: 'early',      // 减脂从初期配额起步，为递减留出头寸
carbPer: null,           // null = 按配额表取值，不预置任何覆盖
fatPer: null
```

---

## 二、契约 ②：配额表常量结构

### 2.1 完整常量对象字面量

```js
/* quota-table.js — 官方配额表（来源：减脂方法论改造.md §1.1 / §1.2 + 官方 Excel 第 1–9 表）
 * 单位：g/kg 体重。所有区间统一为 [下界, 上界]，取值默认取下界（见 §2.2）。
 */

/* 日类型 */
export const DAY_TYPES = ['train', 'rest', 'none'];

/* 七种力训时间点（与 Excel 第 1–7 张减脂表一一对应，见 §1.3） */
export const TRAIN_SLOTS = [
  { id: 'breakfast_early', label: '早饭后练（早起版）' },
  { id: 'breakfast_late',  label: '早饭后练（晚起版）' },
  { id: 'before_lunch',    label: '午饭前练' },
  { id: 'after_lunch',     label: '午饭后练' },
  { id: 'before_dinner',   label: '晚饭前练' },
  { id: 'after_dinner',    label: '晚饭后练' },
  { id: 'night',           label: '夜里练' }
];

/* 配额表：QUOTA_TABLE[phase][sex][dayType] → { carb, protein, fat }
 * - 每项是 [下界, 上界] 的 g/kg 区间
 * - 减脂训练日的 carb 是对象 { early, late }，对应碳水递减的初期 / 末期两档
 * - 减脂限定由 phase 在计算层承载（BMI_ADJUST 只挂在 cut 下），本表不含 bmiAdjust 之类的标记字段 */
export const QUOTA_TABLE = {
  gain: {
    m: {
      train: { carb: [3.5, 4.5], protein: [1.5, 2.0], fat: [1.0, 1.0] },
      rest:  { carb: [2.5, 3.0], protein: [1.5, 2.0], fat: [1.0, 1.0] },
      none:  { carb: [2.5, 3.0], protein: [1.5, 2.0], fat: [1.0, 1.0] }
    },
    f: {
      train: { carb: [3.0, 3.5], protein: [1.5, 1.5], fat: [1.0, 1.0] },
      rest:  { carb: [2.5, 3.0], protein: [1.5, 1.5], fat: [1.0, 1.0] },
      none:  { carb: [2.5, 3.0], protein: [1.5, 1.5], fat: [1.0, 1.0] }
    }
  },
  cut: {
    m: {
      train: { carb: { early: [2.5, 3.0], late: [2.0, 2.5] }, protein: [1.5, 1.5], fat: [0.8, 0.8] },
      rest:  { carb: [1.5, 2.0], protein: [1.5, 1.5], fat: [0.8, 0.8] },
      none:  { carb: [1.5, 2.0], protein: [1.5, 1.5], fat: [0.8, 0.8] }
    },
    f: {
      train: { carb: { early: [2.5, 3.0], late: [2.0, 2.5] }, protein: [1.2, 1.5], fat: [0.8, 0.8] },
      rest:  { carb: [1.5, 2.0], protein: [1.2, 1.5], fat: [0.8, 0.8] },
      none:  { carb: [1.5, 2.0], protein: [1.2, 1.5], fat: [0.8, 0.8] }
    }
  }
};

/* BMI 降配表（仅减脂期生效）：按数组顺序取第一个命中的档，故 32 必须排在 28 前面。
 * 官方给的是「起始」绝对值（点值，非区间），且只给三个数字：碳水 / 蛋白 / 脂肪。 */
export const BMI_ADJUST = {
  cut: [
    { bmiOver: 32, m: { carb: 2.0, protein: 1.0, fat: 0.5 }, f: { carb: 1.7, protein: 1.0, fat: 0.5 } },
    { bmiOver: 28, m: { carb: 2.5, protein: 1.2, fat: 0.6 }, f: { carb: 2.1, protein: 1.2, fat: 0.6 } }
  ]
};

/* 碳水调整尺度（唯一的热量变量） */
export const CARB_STEP = 0.5;   // g/kg 一档 —— 改造 md §1.2「调整尺度」原文

/* BMI 分档阈值（与 Excel 表头注释一致：正常 18.5–24 / 超重 24–28 / 肥胖 >28） */
export const BMI_OVERWEIGHT = 28;
export const BMI_OBESE = 32;
```

> **关于 `bmiAdjust` 字段（T-121 修正）**：早期版本曾在 `QUOTA_TABLE` 的每个格子写 `bmiAdjust: false / true` 来标记该格是否参与 BMI 降配。**该字段已删除，`QUOTA_TABLE` 只含 `carb` / `protein` / `fat`。** 原因是「减脂限定」这一语义由 `phase` 在计算层承载——`BMI_ADJUST` 常量只挂在 `cut` 下（见上方常量字面量），查表时 `phase !== 'cut'` 天然拿不到修正档，等价于 `bmiAdjust: false`。这是**语义等价的设计性省略**，不是契约变更（见 §7.1）。

### 2.2 每层键的命名理由与取值规则

| 层级 | 键 | 命名理由 |
|---|---|---|
| 第 1 层 | `gain` / `cut` | 直接对应 `settings.phase` 的枚举值，避免「中文键 + 英文枚举」的二次映射；`gain`/`cut` 是健身圈通用术语，不会与 `weight`/`target` 之类的既有键名混淆 |
| 第 2 层 | `m` / `f` | 沿用**既有** `settings.sex` 的取值（[constants.js#L72](file:///d:/02工作/健身助手/frontend/src/constants.js#L72) `sex: 'm'`，[utils.js#L27](file:///d:/02工作/健身助手/frontend/src/utils.js#L27) `s.sex === 'f'`）。不另造 `male`/`female`，因为 `utils.js` 里已有 `s.sex === 'f'` 的三元判断，换名会引起全链路回归 |
| 第 3 层 | `train` / `rest` / `none` | 与 `settings.dayType` 的枚举值**逐字相同**，`QUOTA_TABLE[phase][sex][settings.dayType]` 可以直查，不需要 switch 映射 |
| 第 4 层 | `carb` / `protein` / `fat` | 与 `calcProfile` 的返回字段（`{c,p,f}`）和 `QUOTA_TABLE` 的语义一致；此处用全称而非 `c/p/f`，是因为这一层是**常量表**（人要读、要对表），短名只用在计算层的返回值上 |
| 修正层 | `BMI_ADJUST.cut` | 挂在 `cut` 下而不是平铺，理由是官方修正项只针对减脂期；数组 + `bmiOver` 便于将来追加 `bmiOver: 35` 档而不改结构。**「哪些格子参与 BMI 降配」不在 `QUOTA_TABLE` 里逐格标记**（无 `bmiAdjust` 字段），而是由 `phase` 决定查不查得到这一层，语义等价（见 §2.1 注） |

**取值规则（冻结）**：

1. **默认取值 = 区间下界。** 训练日碳水区间 `[2.5, 3.0]` → 默认 2.5 g/kg。理由：减脂必须做碳水递减，从初期区间下界起步才留出向下调整的头寸；同时 T-104 验收要求「减脂期男 70kg → 碳水 175–210g」，175 即 2.5×70。
2. **区间只影响展示与调整上下限，不影响存储。** 引擎只吐一个数（`carbPer` / `proteinPer` / `fatPer`），区间随返回值一起给前端，用于渲染「碳水 2.5（区间 2.5–3）」。
3. **休息日与无训练不使用 `early`/`late`。** `cut` 的 `rest` / `none` 只有一条区间 `[1.5, 2.0]`——这是口径决定 1（取休息日专页绝对值）的直接结果。
4. **BMI 判定用 `weight / (height/100)²`**，`weight` 必须是 `calcQuotaProfile(settings, bodyWeight)` 的入参 `bodyWeight`（与 TDEE 模式的体重口径统一，见 §6.2）。

### 2.3 碳水日内的「餐序位置比例表」

比例来源 = 官方视频四类餐的配额（早饭两成 / 练前餐两成 / 练后餐四成 / 其他餐两成，[减脂方法论改造.md#L50](file:///d:/02工作/健身助手/减脂方法论改造.md#L50)、逐字稿 [#L88-L92](file:///d:/02工作/健身助手/好人松松调研/逐字稿-B站版健身新手的饮食完整手册.md)），映射到 §1.3 的七条官方餐序上。

**推导规则（可手工复算）**：

1. 单位制：`早饭 = 2 单位`、`练前餐 = 2 单位`、`练后餐 = 4 单位`、任一`其他餐 = 2 单位`，共 10 单位 = 100%。
2. **一个餐次身兼两角时，单位相加**（如「早饭=练前餐」= 2+2 = 4 单位 = 40%）。
3. **「其他餐」有多个餐次时，按各 1 单位分摊**（`breakfast_early` 的午饭与晚饭都是其他餐 → 各 1 单位 = 10%，与逐字稿「你的午饭和晚饭都是其他餐，碳水比较少」一致）。
4. `night`（夜里练）无独立练前餐，按第 17 问注释「各餐可以比较均摊，练后餐稍大一点即可，否则白天会太饿」，不把 4 单位并入早饭，而是保持 `20/20/20/40`。

```js
/* 碳水日内比例：CARB_RATIO[dayType]，训练日再按 trainSlot 取。
 * 每个元素是 [mealSlot, 比例]，**数组顺序即当天的用餐先后**（order 由下标 +1 得出）。
 * 不变量：每条比例序列的 Σ = 1（精确到 1e-6），由常量自检脚本保证。 */
export const CARB_RATIO = {
  train: {
    breakfast_early: [['breakfast', 0.40], ['post', 0.40], ['lunch', 0.10], ['dinner', 0.10]],
    breakfast_late:  [['breakfast', 0.40], ['lunch', 0.40], ['dinner', 0.20]],
    before_lunch:    [['breakfast', 0.20], ['pre', 0.20], ['lunch', 0.40], ['dinner', 0.20]],
    after_lunch:     [['breakfast', 0.20], ['lunch', 0.20], ['post', 0.40], ['dinner', 0.20]],
    before_dinner:   [['breakfast', 0.20], ['lunch', 0.20], ['pre', 0.20], ['dinner', 0.40]],
    after_dinner:    [['breakfast', 0.20], ['lunch', 0.20], ['dinner', 0.20], ['post', 0.40]],
    night:           [['breakfast', 0.20], ['lunch', 0.20], ['dinner', 0.20], ['post', 0.40]]
  },
  rest: [['breakfast', 0.30], ['lunch', 0.30], ['dinner', 0.30], ['snack', 0.10]],
  none: [['breakfast', 0.30], ['lunch', 0.30], ['dinner', 0.30], ['snack', 0.10]]
};

/* 各餐次在当天扮演的角色（用于标注「哪个餐次是练前餐 / 练后餐」），
 * 与 CARB_RATIO 同源、逐条对应；角色取值域：breakfast | pre | post | other */
export const SLOT_ROLES = {
  train: {
    breakfast_early: { breakfast: ['breakfast', 'pre'], post: ['post'], lunch: ['other'], dinner: ['other'] },
    breakfast_late:  { breakfast: ['breakfast', 'pre'], lunch: ['post'], dinner: ['other'] },
    before_lunch:    { breakfast: ['breakfast'], pre: ['pre'], lunch: ['post'], dinner: ['other'] },
    after_lunch:     { breakfast: ['breakfast'], lunch: ['pre'], post: ['post'], dinner: ['other'] },
    before_dinner:   { breakfast: ['breakfast'], lunch: ['other'], pre: ['pre'], dinner: ['post'] },
    after_dinner:    { breakfast: ['breakfast'], lunch: ['other'], dinner: ['pre'], post: ['post'] },
    night:           { breakfast: ['breakfast'], lunch: ['other'], dinner: ['other'], post: ['post'] }
  },
  rest: { breakfast: ['breakfast'], lunch: ['other'], dinner: ['other'], snack: ['other'] },
  none: { breakfast: ['breakfast'], lunch: ['other'], dinner: ['other'], snack: ['other'] }
};

/* 休息日 / 无训练的碳水比例依据（改造 md §2.2）：
 * 30/30/30/10 —— 其中 10%（零食/夜宵）有官方原文依据（「这 10% 的少量碳水是用来抵扣本列
 * 未计入的牛奶、蔬菜、调料里的碳水」），三个 30% 是「总量 100% − 10% 后均摊」的结果。
 * 蛋白与脂肪不做碳水式的集中分配，直接均摊（各 25%），见 §3.2 算法第 4/5 步。 */
```

### 2.4 `calcQuotaProfile()` 签名（T-104 实现，本契约冻结形状）

```js
/**
 * 配额派计算：查表得到三大营养素克数；热量是结果而不是输入。
 * @param {object} settings    settings 全量对象
 * @param {number} bodyWeight  体重 kg。weightTrack 开启时由 store 传最近一条 weights.kg，
 *                             否则传 settings.weight —— 与 calcProfile 完全同一口径
 * @returns {{
 *   c: number, p: number, f: number,          // 克数（round1）
 *   kcal: number,                             // 派生展示值 = round(4c + 4p + 9f)，不参与反推
 *   per:  { carb: number, protein: number, fat: number },   // 实际取用的 g/kg
 *   band: { carb: [number, number], protein: [number, number], fat: [number, number] }, // 区间
 *   bmi: number,                              // 入参体重算出的 BMI（round1）
 *   bmiAdjust: null | { bmiOver: number }     // 命中的 BMI 修正档；null = 未命中
 * }}
 */
export function calcQuotaProfile(settings, bodyWeight)
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `c` / `p` / `f` | number | 全天碳水 / 蛋白 / 脂肪克数，`round1`，供 `mealTargets` 直接消费 |
| `kcal` | number | `round(4*c + 4*p + 9*f)`。**派生展示值**，既有红黄绿 UI（[store.js#L47-L57](file:///d:/02工作/健身助手/frontend/src/store.js#L47-L57)）需要它，但它不参与任何配额反推 |
| `per.carb` 等 | number | 实际取用的 g/kg（可能来自 `carbPer` 覆盖或 BMI 修正），用于设置页回显 |
| `band.carb` 等 | [number, number] | 该档的官方区间。BMI 修正命中时上界 = 下界 = 修正值 |
| `bmi` | number | `round1(weight / (height/100)²)` |
| `bmiAdjust` | null \| object | 命中档的 `{ bmiOver }`，未命中为 `null`；UI 据此显示「BMI 修正已接管蛋白/脂肪配额」 |

---

## 三、契约 ③：`mealTargets()` 输入输出结构

### 3.1 函数签名

```js
/**
 * 把全天配额拆到各餐次。
 * @param {('train'|'rest'|'none')} dayType
 * @param {string|null} trainSlot   TRAIN_SLOTS 七值之一；dayType !== 'train' 时必须传 null
 * @param {{c:number, p:number, f:number}} quota  全天配额克数（calcQuotaProfile 的 c/p/f）
 * @returns {{
 *   dayType: string,
 *   trainSlot: string|null,
 *   quota: { c:number, p:number, f:number },
 *   meals: Array<{
 *     order: number,          // 1-based 用餐先后
 *     slot: string,           // MEAL_SLOTS 之一
 *     roles: string[],        // SLOT_ROLES 的角色数组，如 ['breakfast','pre']
 *     carbRatio: number,      // 该餐碳水占全天的比例（Σ = 1）
 *     c: number, p: number, f: number   // 该餐的碳水/蛋白/脂肪目标（克，round1）
 *   }>,
 *   preSlot: string|null,     // 扮演练前餐的餐次；无则 null
 *   postSlot: string|null,    // 扮演练后餐的餐次；无则 null
 *   constraints: { postProtein: [number, number], postFatMax: number }  // 固定 [30, 50] / 20
 * }}
 */
export function mealTargets(dayType, trainSlot, quota)
```

**契约要点**

- 输入只有三样：日类型、训练时间点、全天配额。**不接收** weight / sex / phase —— 那三项已在 `calcQuotaProfile` 里消化完，`mealTargets` 是纯分配函数（便于 T-110 独立断言）。
- `roles` 让调用方**不必再查 `SLOT_ROLES`** 就能知道哪个餐次是练前餐 / 练后餐；`preSlot` / `postSlot` 是它的降维投影，供今日页直接定位。
- 返回值中**不含** `kcal`：分餐是碳水/蛋白/脂肪的分配，热量由页面按 `4c+4p+9f` 自行派生。

### 3.2 分配算法（冻结，必须逐条实现）

```
输入 quota = {c, p, f}，ratios = CARB_RATIO[dayType]（训练日再取 [trainSlot]）
N = ratios.length

第 1 步 · 碳水
  对 i = 1..N−1:  meals[i].c = round1(quota.c × ratios[i].ratio)
  对 i = N:        meals[N].c = round1(quota.c − Σ meals[1..N−1].c)   // 末项吃余数，保证 Σ ≡ quota.c

第 2 步 · 蛋白
  p_each = quota.p / N
  若存在 postSlot:
      post.p = min(quota.p, max(30, min(50, p_each)))    // 练后餐蛋白 30–50g
      其余餐次 × (N−1) 均分 (quota.p − post.p)
  否则:
      各餐均分 p_each
  取整：前 N−1 项 round1，末项 = round1(quota.p − Σ前项)

第 3 步 · 脂肪
  f_each = quota.f / N
  若存在 postSlot:
      post.f = min(f_each, 20)                            // 练后餐脂肪 ≤20g
      其余餐次 × (N−1) 均分 (quota.f − post.f)
  否则:
      各餐均分 f_each
  取整：前 N−1 项 round1，末项 = round1(quota.f − Σ前项)

第 4 步 · 休息日 / 无训练（dayType !== 'train'）
  无 postSlot，蛋白与脂肪天然走「均分」分支，不做碳水集中 —— 满足「各餐碳蛋脂大概均摊」
```

**为什么末项吃余数**：`round1` 会让 Σ 与全天配额差 0.1–0.3 g，而 T-107 的验收要求「各餐相加 = 全天 100%」。把余数并入 `order` 最大（当天最后一餐）的项，是唯一不破坏其他餐取整规则的做法。

### 3.3 示例 A：训练日（练后餐约束生效）

**输入**

```js
calcQuotaProfile(
  { phase: 'cut', sex: 'm', dayType: 'train', carbStage: 'early',
    carbPer: null, proteinPer: 1.5, fatPer: null, weight: 70, height: 175 },
  70
)  // → { c: 175, p: 105, f: 56, kcal: 1624, per: {carb: 2.5, protein: 1.5, fat: 0.8}, ... }

mealTargets('train', 'before_dinner', { c: 175, p: 105, f: 56 })
```

**输出**

```json
{
  "dayType": "train",
  "trainSlot": "before_dinner",
  "quota": { "c": 175, "p": 105, "f": 56 },
  "meals": [
    { "order": 1, "slot": "breakfast", "roles": ["breakfast"], "carbRatio": 0.20, "c": 35, "p": 25, "f": 14 },
    { "order": 2, "slot": "lunch",     "roles": ["other"],     "carbRatio": 0.20, "c": 35, "p": 25, "f": 14 },
    { "order": 3, "slot": "pre",       "roles": ["pre"],       "carbRatio": 0.20, "c": 35, "p": 25, "f": 14 },
    { "order": 4, "slot": "dinner",    "roles": ["post"],      "carbRatio": 0.40, "c": 70, "p": 30, "f": 14 }
  ],
  "preSlot": "pre",
  "postSlot": "dinner",
  "constraints": { "postProtein": [30, 50], "postFatMax": 20 }
}
```

**数字校验**：碳水 `35+35+35+70 = 175` ✔；蛋白 `25+25+25+30 = 105` ✔（`p_each = 26.25 < 30`，练后餐被抬到下限 30，其余三顿由 26.25 降到 25）；脂肪 `14×4 = 56` ✔（`f_each = 14 ≤ 20`，练后餐不触发上限）。全天热量 = `4×175 + 4×105 + 9×56 = 1624 kcal`。

> 练后餐为 `dinner`，与 T-107 验收「trainSlot=晚饭前练 → 晚饭为练后餐且占碳水 40%」一致。

### 3.4 示例 B：增肌日（练后餐脂肪上限生效）

```js
// 增肌 男 90kg，trainSlot = after_dinner
calcQuotaProfile({ phase: 'gain', sex: 'm', dayType: 'train', proteinPer: 1.5,
                   carbPer: null, fatPer: null, weight: 90, height: 180 }, 90)
// → { c: 315, p: 135, f: 90, per: {carb: 3.5, protein: 1.5, fat: 1.0}, ... }
```

```json
{
  "dayType": "train", "trainSlot": "after_dinner",
  "quota": { "c": 315, "p": 135, "f": 90 },
  "meals": [
    { "order": 1, "slot": "breakfast", "roles": ["breakfast"], "carbRatio": 0.20, "c": 63, "p": 33.8, "f": 23.3 },
    { "order": 2, "slot": "lunch",     "roles": ["other"],     "carbRatio": 0.20, "c": 63, "p": 33.8, "f": 23.3 },
    { "order": 3, "slot": "dinner",    "roles": ["pre"],       "carbRatio": 0.20, "c": 63, "p": 33.6, "f": 23.4 },
    { "order": 4, "slot": "post",      "roles": ["post"],      "carbRatio": 0.40, "c": 126, "p": 33.8, "f": 20.0 }
  ],
  "preSlot": "dinner", "postSlot": "post",
  "constraints": { "postProtein": [30, 50], "postFatMax": 20 }
}
```

**数字校验**：脂肪 `f_each = 22.5 > 20` → 练后餐（post）被硬约束压到 `20.0`，其余三顿 `(90−20)/3 = 23.333 → 23.3`；余数 `90 − 20.0 − 23.3×3 = 0.1` 由**最后一个非练后餐**（dinner）吸收 → `23.4`。蛋白 `p_each = 33.75 → 33.8`，`33.8×4 = 135.2` 超出 `0.2`，同样由 dinner 吸收 → `33.6`。碳水 `63×3 + 126 = 315` ✔。合计 `c 315 / p 135 / f 90` ✔，全天热量 `4×315 + 4×135 + 9×90 = 2610 kcal`。

> 本示例的数值取自实现实跑（`mealTargets('train','after_dinner',{c:315,p:135,f:90})`），**练后餐不参与吃余数**，余数由最后一个非练后餐吸收（取舍依据见 §8.4）。

### 3.5 示例 C：无训练 / 休息日

```js
calcQuotaProfile({ phase: 'cut', sex: 'm', dayType: 'none', proteinPer: 1.5,
                   carbPer: null, fatPer: null, weight: 70, height: 175 }, 70)
// → { c: 105, p: 105, f: 56, per: {carb: 1.5, protein: 1.5, fat: 0.8}, ... }

mealTargets('none', null, { c: 105, p: 105, f: 56 })
```

```json
{
  "dayType": "none", "trainSlot": null,
  "quota": { "c": 105, "p": 105, "f": 56 },
  "meals": [
    { "order": 1, "slot": "breakfast", "roles": ["breakfast"], "carbRatio": 0.30, "c": 31.5, "p": 26.3, "f": 14 },
    { "order": 2, "slot": "lunch",     "roles": ["other"],     "carbRatio": 0.30, "c": 31.5, "p": 26.3, "f": 14 },
    { "order": 3, "slot": "dinner",    "roles": ["other"],     "carbRatio": 0.30, "c": 31.5, "p": 26.3, "f": 14 },
    { "order": 4, "slot": "snack",     "roles": ["other"],     "carbRatio": 0.10, "c": 10.5, "p": 26.1, "f": 14 }
  ],
  "preSlot": null, "postSlot": null,
  "constraints": { "postProtein": [30, 50], "postFatMax": 20 }
}
```

**数字校验**：碳水 `31.5×3 + 10.5 = 105` ✔；蛋白无练后餐 → 均分 `26.25`，前三项 `26.3`、末项吃余数 `105 − 78.9 = 26.1` ✔；脂肪 `14×4 = 56` ✔。无练前/练后餐 ✔。

### 3.6 七种训练时间点的比例速查表（自检用）

| trainSlot | ① 早饭 | ② 午饭 | ③ 练前餐 | ④ 练后餐 | ⑤ 晚饭 | Σ |
|---|---|---|---|---|---|---|
| `breakfast_early` | 0.40（早饭=练前） | 0.10（其他） | — | 0.40 | 0.10（其他） | 1.00 |
| `breakfast_late` | 0.40（早饭=练前） | 0.40（练后） | — | — | 0.20（其他） | 1.00 |
| `before_lunch` | 0.20 | 0.40（练后） | 0.20 | — | 0.20（其他） | 1.00 |
| `after_lunch` | 0.20 | 0.20（练前） | — | 0.40 | 0.20（其他） | 1.00 |
| `before_dinner` | 0.20 | 0.20（其他） | 0.20 | — | 0.40（练后） | 1.00 |
| `after_dinner` | 0.20 | 0.20（其他） | — | 0.40 | 0.20（练前） | 1.00 |
| `night` | 0.20 | 0.20（其他） | — | 0.40 | 0.20（其他） | 1.00 |

---

## 四、契约 ④：餐次分包数据形状与迁移

### 4.1 两个候选结构

#### 方案 A —— 保留扁平 `items`，新增 `mealAllocation` 映射（**推荐**）

```json
{
  "id": 3,
  "name": "虾仁炒饭",
  "portions": 6,
  "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
  "locked": [],
  "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 },
  "totals": { "kcal": 2716.9, "protein": 118.9, "carbs": 431.2, "fat": 75.1 },
  "created_at": "2026-09-18 09:12:00",
  "updated_at": "2026-09-18 09:12:00"
}
```

- `items` 仍是**整锅食材克重**（扁平 JSON），`portions` 仍是**总份数**；两者语义与今天完全一致。
- `mealAllocation` 是**新增的一层映射**：`餐次 → 该餐取走的份数`。
- 「不同克重」由份数差表达：`该餐克重 = mealAllocation[slot] × (items / portions)`。例：`dinner` 拿 2.4 份 = 每份克重的 2.4 倍。
- `{}` = **未分包**（旧配方的默认态，等价于「全部份数归正餐」）。

#### 方案 B —— 嵌套 `items`（每餐独立配比）

```json
{
  "portions": 6,
  "meals": {
    "breakfast": { "rice": 80, "egg": 100 },
    "lunch":     { "rice": 120, "chicken_breast": 100, "broccoli": 100 },
    "pre":       { "rice": 60 },
    "dinner":    { "rice": 250, "chicken_breast": 200, "oil": 20 }
  }
}
```

### 4.2 两方案对比与推荐

| 维度 | 方案 A（扁平 + `mealAllocation`） | 方案 B（嵌套 `items`） |
|---|---|---|
| 能否表达「不同克重」 | ✅ 由份数差表达（1.2 份 / 2.4 份），`inventory.portions` 本就是 REAL（[db.js#L38](file:///d:/02工作/健身助手/backend/src/db.js#L38)），可直接承载小数份 | ✅ 直接给每餐克数 |
| 能否表达「不同配比」 | ❌（也不需要，见下） | ✅ |
| 物理可行性 | ✅ 同一锅是均质混合物，**物理上只能拆出不同克重、拆不出不同配比** | ⚠️ 表达「各餐配比不同」需要**分锅做**，与「一锅出」的备餐前提冲突 |
| `calcRecipeTotals` | 零改动（入参仍是扁平 items）→ [helpers.js#L34-L46](file:///d:/02工作/健身助手/backend/src/helpers.js#L34-L46) | 必须重写为 Σ 各餐 |
| `calcTotals` / `perOf` | 零改动 → [utils.js#L39-L54](file:///d:/02工作/健身助手/frontend/src/utils.js#L39-L54) | 必须重写 |
| `autoGenerate` | 零改动（按整锅克重分配后再按比例切份）→ [utils.js#L134-L167](file:///d:/02工作/健身助手/frontend/src/utils.js#L134-L167) | 必须改为逐餐分配，两阶段锁定语义全部重定义 |
| `adjustFoodByStep` | 零改动（同类热量守恒仍按整锅算）→ [utils.js#L184-L215](file:///d:/02工作/健身助手/frontend/src/utils.js#L184-L215) | 必须改为「按餐次定向调整」（改造 md §6.1 已点名此事） |
| `locked` 语义 | 不变（整锅级锁定） | 需降级到「每餐每食材」，锁定的可解释性大幅下降 |
| 旧数据迁移 | **零迁移**：缺字段 = `{}` = 未分包 | **有损转换**：扁平 items 无法反推各餐克数，只能编造 |
| 影响面（T-106 估工） | 加列 + 加字段 + 一个 Σ 校验 | 重写计算层 4 个函数 + 迁移 + UI 全改，预估 > 1.5 倍工作量 |

**推荐：方案 A。** 三条理由，按权重排序：

1. **物理约束优先于表达力。** 方案 B 能表达的东西（同一锅里各餐配比不同）在厨房里不存在——一锅炒饭不会因为装进不同餐盒就变成不同配比。让数据结构表达做不到的事，只会在 UI 上诱导用户录出无法执行的配方。
2. **改动面小一个数量级。** 计算层的 5 个核心函数（`calcRecipeTotals` / `calcTotals` / `perOf` / `autoGenerate` / `adjustFoodByStep`）全部以「扁平 items + portions」为输入，方案 A 让它们**一行不改**；方案 B 需要全部重写，而其中 `adjustFoodByStep` 的热量守恒补偿算法（[utils.js#L196-L213](file:///d:/02工作/健身助手/frontend/src/utils.js#L196-L213)）是按「同类整锅总热量」定义的，改到逐餐维度会连带推翻 v2.5 的锁定语义。
3. **迁移成本为零，且语义诚实。** 方案 A 的缺省态 `{}`（未分包）天然容纳全部历史数据；方案 B 必须把扁平 items 硬拆成四份，而「这份是早饭」这个信息在旧数据里根本不存在。

**方案 B 的唯一优势（跨锅补项）本 Sprint 明确不纳入**：练后餐若需要额外的便携快碳或蛋白粉，按改造 md 阶段 2 已定口径——「备餐负责覆盖**计划内餐次**；练后餐若不在备餐餐次内，由『晚加餐 / 蛋白粉』槽位承接」（[减脂方法论改造.md#L248](file:///d:/02工作/健身助手/减脂方法论改造.md#L248)），即走 `day_logs.late` / `day_logs.whey`，不进配方结构。

### 4.3 `recipes` 数据形状与不变量

**建表/迁移** → [backend/src/db.js#L25-L33](file:///d:/02工作/健身助手/backend/src/db.js#L25-L33)、[db.js#L103-L109](file:///d:/02工作/健身助手/backend/src/db.js#L103-L109)

```sql
ALTER TABLE recipes ADD COLUMN meal_allocation TEXT NOT NULL DEFAULT '{}';
```

| 不变量 | 规则 |
|---|---|
| 键域 | `mealAllocation` 的键 ⊆ `PACK_SLOTS`（`breakfast` / `lunch` / `pre` / `post` / `dinner`）。`snack` 不进分包（§0.3） |
| 值域 | 每个值 ≥ 0，且为 **0.1 的整数倍**（与 `round1` 同精度） |
| 守恒 | **Σ values = `portions`**，容差 `1e-6`；违反 → 400 |
| 空态 | `{}` = 未分包（合法，旧数据态）。不允许「部分分包」（Σ ≠ portions 且 ≠ 0） |
| 唯一性 | 对象键天然唯一，不需要额外校验 |

**API 请求 / 响应示例**

```http
POST /api/recipes
Content-Type: application/json
```

```json
{
  "name": "虾仁炒饭",
  "portions": 6,
  "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
  "locked": [],
  "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 }
}
```

```json
{
  "code": 0,
  "data": {
    "id": 3,
    "name": "虾仁炒饭",
    "portions": 6,
    "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
    "locked": [],
    "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 },
    "totals": { "kcal": 2716.9, "protein": 118.9, "carbs": 431.2, "fat": 75.1 },
    "created_at": "2026-09-18 09:12:00",
    "updated_at": "2026-09-18 09:12:00"
  }
}
```

**响应 400（守恒被破坏）**

```json
{ "code": 1, "message": "mealAllocation 份数之和必须等于 portions（6），当前为 5.6" }
```

**响应 400（键越界）**

```json
{ "code": 1, "message": "mealAllocation 的餐次只允许 breakfast/lunch/pre/post/dinner" }
```

**`PUT /api/recipes/:id` 的缺省语义（照抄 `locked` 的既有写法）**：`mealAllocation` 未传时**保留原值**，不置空 —— 与 [recipes.js#L80](file:///d:/02工作/健身助手/backend/src/routes/recipes.js#L80) `req.body.locked ?? JSON.parse(row.locked || '[]')` 同构，避免旧客户端一次 PUT 清掉分包。

### 4.4 `inventory` 数据形状与不变量

| 现状 | 位置 |
|---|---|
| `items TEXT`（整锅食材克重 JSON，锅位队列展示用） | [db.js#L35-L42](file:///d:/02工作/健身助手/backend/src/db.js#L35-L42)、[db.js#L96-L101](file:///d:/02工作/健身助手/backend/src/db.js#L96-L101) |
| `portions REAL`（份数，允许小数） | [db.js#L38](file:///d:/02工作/健身助手/backend/src/db.js#L38) |
| 登记批次 `POST /api/inventory` | [inventory.js#L66-L84](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L66-L84) |
| FIFO 核销 `POST /api/inventory/consume` | [inventory.js#L91-L122](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L91-L122) |
| 回撤 `POST /api/inventory/restore` | [inventory.js#L137-L150](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L137-L150) |

**新增列**

```sql
ALTER TABLE inventory ADD COLUMN meal_allocation TEXT;   -- 可空，NULL = 未分包批次
```

| 不变量 | 规则 |
|---|---|
| 语义 | `meal_allocation` 存的是**剩余份数**（随核销递减），不是初始分配 |
| 守恒 | 非 NULL 时 **Σ values = `portions`**，容差 `1e-6`；同一事务内同步维护 |
| NULL 与 `{}` 的区别 | `NULL` = **未分包**（旧批次，份数可被任意餐次取用，见 §4.6 规则 3）；`{}` 是**非法态**（已分包但零份），服务端拒收 |

**`POST /api/inventory` 请求（新增 `mealAllocation` 可选字段）**

```json
{
  "name": "虾仁炒饭 锅",
  "portions": 6,
  "perKcal": 452.8, "perP": 19.8, "perC": 71.9, "perF": 12.5,
  "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
  "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 }
}
```

- `mealAllocation` 缺省 / `null` → 落库 `NULL`（未分包批次，与今天行为一致，老客户端不受影响）
- 给了 → 校验键域 + Σ = `portions`，违反 → 400

**响应（`buildBatch` 增加 `mealAllocation` 字段）**

```json
{
  "code": 0,
  "data": {
    "batch": {
      "id": "b-0918-1", "name": "虾仁炒饭 锅", "portions": 6, "inAt": "09-18 09:30",
      "perKcal": 452.8, "perP": 19.8, "perC": 71.9, "perF": 12.5,
      "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
      "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 }
    },
    "inventory": [ "/* 全量库存列表，结构同上 */" ]
  }
}
```

### 4.5 `/api/inventory/consume` 按餐次核销

**请求**

```json
{ "portions": 1.2, "mealSlot": "dinner" }
```

| 字段 | 类型 | 必填 | 校验 | 缺省语义 |
|---|---|---|---|---|
| `portions` | number | 是 | `> 0`（沿用 [inventory.js#L93](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L93)） | — |
| `mealSlot` | string | 否 | ∈ `PACK_SLOTS`，否则 400 | 缺省 / `null` = 未指定餐次，**完全走现有 FIFO 行为**（向后兼容，老客户端不改也能用） |

**响应**

```json
{
  "code": 0,
  "data": {
    "inventory": [
      {
        "id": "b-0918-1", "name": "虾仁炒饭 锅", "portions": 4.8, "inAt": "09-18 09:30",
        "perKcal": 452.8, "perP": 19.8, "perC": 71.9, "perF": 12.5,
        "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
        "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 1.2 }
      }
    ],
    "consumed": 1.2,
    "shortage": 0,
    "detail": [
      {
        "batchId": "b-0918-1", "batchName": "虾仁炒饭 锅",
        "mealSlot": "dinner", "fromUnassigned": false,
        "per": { "kcal": 543.4, "p": 23.8, "c": 86.3, "f": 15.0 }
      }
    ]
  }
}
```

**扣减规则（冻结，四条）**

1. `mealSlot` 缺省 / `null`：完全沿用现有逻辑（[inventory.js#L95-L119](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L95-L119)），只改 `portions`，`detail[].mealSlot = null`、`fromUnassigned = false`。
2. `mealSlot` 给定：**只在 `meal_allocation` 非 NULL 且 `meal_allocation[mealSlot] > 0` 的批次里**按 `rowid ASC` 扣减；每扣一次**同事务同时**递减 `meal_allocation[mealSlot]` 与 `portions`（保证 Σ 不变量），`fromUnassigned = false`。
3. 已分包批次的该餐次余额不足：继续扫 `meal_allocation IS NULL` 的旧批次（未分包池）按 FIFO 扣，扣减只改 `portions`（**不写 `meal_allocation`**，不给旧数据编造餐次），`detail[].fromUnassigned = true`，前端据此刻提示「本份来自未分包批次」。
4. 仍不足：`shortage` = 剩余未扣份数，事务照常提交已扣部分（与现有语义一致：库存不足按现有份数记录，前端 toast 见 [TodayView.vue#L111](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L111)）。**不跨餐次借用**——分包的意义就是餐次隔离，借用会让分包失去约束力。

**`POST /api/inventory/restore`（对称扩展）**

```json
{
  "items": [
    { "batchId": "b-0918-1", "portions": 1.2, "mealSlot": "dinner" }
  ]
}
```

| 情形 | 行为 |
|---|---|
| 带 `mealSlot` 且该批次 `meal_allocation` 非 NULL | 同时 `meal_allocation[slot] += portions`、`portions += portions` |
| 带 `mealSlot` 但该批次为 NULL（未分包） | **只回补 `portions`**，保持 `meal_allocation = NULL`（不编造餐次） |
| 不带 `mealSlot` | 只回补 `portions`（现有行为，[inventory.js#L141-L147](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L141-L147)） |

> 回撤必须与核销严格对称，否则会重演 CALC-AUDIT §3.3 记录的「库存（实物真相）与记录（记录真相）分叉」事故（[CALC-AUDIT.md#L109-L125](file:///d:/02工作/健身助手/CALC-AUDIT.md#L109-L125)）。

### 4.6 `day_logs` 的餐次落点

现有四槽位 → [db.js#L49-L61](file:///d:/02工作/健身助手/backend/src/db.js#L49-L61)、校验 → [day-logs.js#L29-L49](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L29-L49)

| 契约餐次（§0.3） | day_logs 落点 | 是否记份数 |
|---|---|---|
| `breakfast` | `breakfast`（加餐条目数组）**＋** `meals` / `mealsLog[slot='breakfast']`（当早饭来自备餐锅时） | 两者都有 |
| `lunch` | `meals` / `mealsLog[slot='lunch']` | 是 |
| `pre` | `meals` / `mealsLog[slot='pre']` | 是 |
| `post` | `meals` / `mealsLog[slot='post']` | 是 |
| `dinner` | `meals` / `mealsLog[slot='dinner']` | 是 |
| `snack` | `late`（加餐条目数组，**唯一落点**） | 否（条目式记克数） |
| （加项，非餐次） | `whey`（蛋白粉勺数 0–6） | — |

**两处契约变更**

1. **`meals_log` 事件新增 `slot` 字段**（可空）

```json
[
  { "ts": 1758103800000, "batchId": "b-0918-1", "batchName": "虾仁炒饭 锅", "slot": "dinner",
    "per": { "kcal": 543.4, "p": 23.8, "c": 86.3, "f": 15.0 } }
]
```

- 校验：`slot` 存在时必须 ∈ `PACK_SLOTS`，否则 400 `mealsLog 条目的 slot 只允许 breakfast/lunch/pre/post/dinner`
- 旧事件无 `slot` → 保留为 `null`，前端展示为「正餐（未指定餐次）」。现 [day-logs.js#L59-L63](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L59-L63) 的 `normalizeMealsLog` 只过滤缺营养的条目，需**额外保留 `slot` 字段**（不能像现在这样只透传 `per`/`batchId`）

2. **`meals` 上限从 0–4 放宽到 0–8**

`meals` 的语义由「正餐份数（午饭 + 晚饭，故 0–4）」扩展为「**全部由备餐锅分包核销的份数之和**」。训练日最多 4 个可分餐次 × 每餐 ≤2 份 = 8。

> **T-111 补充**：`meals` 允许最多 1 位小数（0.1 粒度）——「份」是每餐从锅里盛出的量（如早饭 1.2 份、练后餐 2.4 份），不要求整数；列类型同步为 `REAL`，后端校验为「0–8 且最多 1 位小数」，详见 §8.1。

| 需同步修改的位置 | 现状 |
|---|---|
| [backend/src/routes/day-logs.js#L30-L32](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L30-L32) | `meals 必须为 0-4 的整数` |
| [frontend/src/views/TodayView.vue#L21](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L21) | `const MEAL_MAX = 4` |
| [TodayView.vue#L168](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L168) | `v = Math.max(0, Math.min(MEAL_MAX, v))` |
| [TodayView.vue#L247](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L247) | `今日正餐 N/{{ MEAL_MAX }} 份` |
| [TodayView.vue#L294](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L294) | `<Stepper :max="MEAL_MAX">` |

> **为什么 `snack` 不进分包 / 不进 `meals`**：改造 md §2.2 写得很明确——零食/夜宵那 10% 是「用来抵扣本列未计入的牛奶、蔬菜、调料里的碳水」（[减脂方法论改造.md#L110](file:///d:/02工作/健身助手/减脂方法论改造.md#L110)），是**机动额度**而非一份备餐盒。它与 `late` 字段（条目式记克数）天然同构，也与「无训练方案没有练前/练后餐、只有四个餐次」的官方结构一致。把 `snack` 也纳入分包只会让同一份实物同时出现在 `meals` 和 `late` 两处，制造新的双计风险。

### 4.7 旧数据迁移策略（**冻结：双读兼容，不做一次性搬迁**）

**结论：双读兼容。** 实现动作只有两个幂等 `ALTER TABLE ADD COLUMN`，不写任何数据。

**迁移代码伪代码** → 追加到 [backend/src/db.js#L97-L109](file:///d:/02工作/健身助手/backend/src/db.js#L97-L109) 的两个既有迁移函数

```js
function migrateRecipesColumns() {
  const cols = db.prepare('PRAGMA table_info(recipes)').all().map(c => c.name);
  if (!cols.includes('locked')) db.exec("ALTER TABLE recipes ADD COLUMN locked TEXT NOT NULL DEFAULT '[]'");
  // T-106：餐次分包映射，DEFAULT '{}' = 未分包，旧配方自动获得该值、无需回填
  if (!cols.includes('meal_allocation')) {
    db.exec("ALTER TABLE recipes ADD COLUMN meal_allocation TEXT NOT NULL DEFAULT '{}'");
  }
}

function migrateInventoryColumns() {
  const cols = db.prepare('PRAGMA table_info(inventory)').all().map(c => c.name);
  if (!cols.includes('items')) db.exec('ALTER TABLE inventory ADD COLUMN items TEXT');
  // T-106：必须可空。NULL = 未分包批次，与 '{}'（已分包但零份的非法态）语义相反，
  // 合并成一个值会让 consume 无法区分「旧批次可自由取用」和「新批次已分光」
  if (!cols.includes('meal_allocation')) {
    db.exec('ALTER TABLE inventory ADD COLUMN meal_allocation TEXT');
  }
}
```

**读路径伪代码**

```js
// backend/src/routes/recipes.js — buildRecipe（现 L46-L58）
function buildRecipe(row) {
  const items = JSON.parse(row.items);
  return {
    id: row.id, name: row.name, portions: row.portions, items,
    locked: JSON.parse(row.locked || '[]'),
    // 旧行 meal_allocation 恒为 '{}'（DEFAULT 补的），解析结果天然是「未分包」
    mealAllocation: JSON.parse(row.meal_allocation || '{}'),
    totals: calcRecipeTotals(items),
    created_at: row.created_at, updated_at: row.updated_at,
  };
}

// backend/src/routes/inventory.js — buildBatch（现 L8-L19）
function buildBatch(row) {
  let items = null;
  if (row.items) { try { items = JSON.parse(row.items); } catch { items = null; } }
  let mealAllocation = null;                       // null = 未分包
  if (row.meal_allocation) {
    try { mealAllocation = JSON.parse(row.meal_allocation); } catch { mealAllocation = null; }
  }
  return { id: row.id, name: row.name, portions: row.portions, inAt: row.in_at,
           perKcal: row.per_kcal, perP: row.per_p, perC: row.per_c, perF: row.per_f,
           items, mealAllocation };
}
```

**为什么双读优于一次性迁移（三条理由）**

1. **旧数据的餐次归属不可推断。** `recipes` / `inventory` 里没有任何字段记录「这份是早饭」。任何一次性迁移都只能编造（把全部份数塞进某一个槽位），等于把假数据写进真库，而且**不可逆**——迁移后无法区分「用户真实分包的」和「迁移编造的」。双读保留了这个区分能力（`NULL` vs 非 NULL）。
2. **项目已有同风格的双读先例**，同一套约定，不引入新范式：
   - `recipes.locked` 缺列补 `'[]'`，旧配方一律视为「全部可变」→ [db.js#L105-L109](file:///d:/02工作/健身助手/backend/src/db.js#L105-L109)
   - `inventory.items` 缺列补 NULL，旧批次只显示营养不显示食材 → [db.js#L97-L101](file:///d:/02工作/健身助手/backend/src/db.js#L97-L101)
   - `meals_log` 为 NULL 时前端按 `per_snap` 旧口径回溯 → [utils.js#L359-L371](file:///d:/02工作/健身助手/frontend/src/utils.js#L359-L371)
3. **零停机、零回滚成本。** 双读只加列不写数据，出问题时删掉读分支即可回退；一次性迁移要么写坏（无法恢复旧语义），要么需要先全库备份。

**若坚持一次性迁移（仅备查，本契约不采用）**

```sql
-- recipes：DEFAULT '{}' 已在 ALTER 时补齐全部旧行，无需 UPDATE。
-- 若列曾以其他默认值建立，用下面这条兜底：
UPDATE recipes SET meal_allocation = '{}'
 WHERE meal_allocation IS NULL OR meal_allocation = '' OR meal_allocation = 'null';

-- inventory：旧批次一律保持 NULL（未分包），绝不编造餐次归属。
-- 反例（禁止执行）：
--   UPDATE inventory SET meal_allocation = json_object('lunch', portions);  -- 编造，不可逆
```

### 4.8 端到端数据结构串联（一次完整链路）

```json
/* 1. 配方（T-108 设定分包） */
{ "id": 3, "portions": 6, "items": { "rice": 510, "chicken_breast": 300, "broccoli": 400, "oil": 60 },
  "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 } }

/* 2. 登记入库（T-106）→ 条目式：锅位队列显示整锅 items，同时记住各餐余额 */
{ "id": "b-0918-1", "portions": 6, "items": { "...": 0 },
  "mealAllocation": { "breakfast": 1.2, "lunch": 1.2, "pre": 1.2, "dinner": 2.4 } }

/* 3. 晚餐核销（T-109）：吃 1.2 份练后餐 */
POST /api/inventory/consume  { "portions": 1.2, "mealSlot": "dinner" }
→ inventory[0].portions = 4.8，mealAllocation.dinner = 1.2
→ detail = [{ "batchId": "b-0918-1", "mealSlot": "dinner", "fromUnassigned": false,
              "per": { "kcal": 543.4, "p": 23.8, "c": 86.3, "f": 15.0 } }]

/* 4. 落打卡（T-109） */
PUT /api/day-logs/2026-09-18
{ "meals": 4.8, "whey": 2, "breakfast": [{ "id": "egg", "g": 100 }], "late": [],
  "consumed": 4.8, "satiety": 4, "checkedIn": 1,
  "mealsLog": [{ "ts": 1758103800000, "batchId": "b-0918-1", "batchName": "虾仁炒饭 锅",
                 "slot": "dinner", "per": { "kcal": 543.4, "p": 23.8, "c": 86.3, "f": 15.0 } }] }

/* 5. 与 mealTargets 对齐（T-108 今日页展示） */
mealTargets('train', 'before_dinner', { c: 175, p: 105, f: 56 })
  → dinners 目标 c:70 / p:30 / f:14，本条实核销 86.3 / 23.8 / 15.0（碳水 +23.3%）
```

> **T-111**：第 4 步的 `"meals": 4.8` 自 T-111 起可直接执行（此前被整数校验 400 拒收），核销与展示全程按 0.1 份粒度，见 §8.1。

---

## 五、对现有代码的影响清单（供 T-103 / T-104 / T-106 / T-108 / T-109 分工）

| # | 位置 | 现状 | 契约要求 | 归属 |
|---|---|---|---|---|
| 1 | [settings.js#L11-L57](file:///d:/02工作/健身助手/backend/src/routes/settings.js#L11-L57) | 白名单 `NUMBER_KEYS` / `STRING_KEYS` + 特例分支 | 新增 8 键（含 `proteinPer` 范围补齐），枚举键单独分支 | T-103 |
| 2 | [seed.js#L53-L58](file:///d:/02工作/健身助手/backend/src/seed.js#L53-L58)、[db.js#L114-L129](file:///d:/02工作/健身助手/backend/src/db.js#L114-L129)、[constants.js#L71-L76](file:///d:/02工作/健身助手/frontend/src/constants.js#L71-L76) | 三处默认值 | 同步追加 §1.7 的 7 个新键 | T-103 / T-104 |
| 3 | [utils.js#L24-L37](file:///d:/02工作/健身助手/frontend/src/utils.js#L24-L37) `calcProfile` | TDEE 派唯一入口 | **保留原样**；新增 `calcQuotaProfile` 并行；`store.profile` 按 `calcMode` 择一 | T-104 |
| 4 | [utils.js](file:///d:/02工作/健身助手/frontend/src/utils.js) `dailyPreview` / `perMealIntake` / `potDaysOf` / `perMealTargets` | 全天固定拆 `per × 2` | 分包**只按各餐比例把这 2 份拆到各餐，日总量不变**（仍为 `每份 × 2 + 加项`），一锅天数 = `⌈份数 ÷ 2⌉`。原写「各餐份数不等，预演需改为 `Σ (每份营养 × 各餐份数)`」的旧口径已被 T-118 推翻，见 §8.6 | T-108 |
| 5 | [store.js#L47-L57](file:///d:/02工作/健身助手/frontend/src/store.js#L47-L57) `previewState` | 用 `dailyPreview` 做四项红黄绿 | 同上；配额模式下目标改为 `calcQuotaProfile` 的 `c/p/f`（`kcal` 为派生值） | T-104 / T-108 |
| 6 | [db.js#L25-L33](file:///d:/02工作/健身助手/backend/src/db.js#L25-L33)、[L97-L109](file:///d:/02工作/健身助手/backend/src/db.js#L97-L109) | 建表 + 幂等补列 | 加 `recipes.meal_allocation NOT NULL DEFAULT '{}'`、`inventory.meal_allocation TEXT` | T-106 |
| 7 | [recipes.js#L39-L84](file:///d:/02工作/健身助手/backend/src/routes/recipes.js#L39-L84) | `assertRecipeBody` / `normalizeItems` / `buildRecipe` | 加 `mealAllocation` 校验（键域 + Σ=portions）、归一化、序列化、缺省保留 | T-106 |
| 8 | [inventory.js#L8-L84](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L8-L84) | `buildBatch` / `assertBatchBody` | 加 `mealAllocation` 读 + 登记校验 | T-106 |
| 9 | [inventory.js#L91-L150](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L91-L150) | `consume` / `restore` | 加 `mealSlot` 参数与 per-slot 扣减/回补（§4.5 四条规则） | T-106 |
| 10 | [day-logs.js#L17-L63](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L17-L63) | `assertMealsLog` / `normalizeMealsLog` / `rowToLog` | 保留并校验 `mealsLog[].slot` | T-106 / T-109 |
| 11 | [day-logs.js#L30-L32](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L30-L32)、[TodayView.vue#L21](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L21) 等 5 处 | `meals` 0–4 硬编码 | 放宽到 0–8 | T-106 / T-109 |
| 12 | [api.js#L60-L67](file:///d:/02工作/健身助手/frontend/src/api.js#L60-L67) `recipeBody` | 收发 `name/portions/items/locked` | 增补 `mealAllocation`（与 `locked` 同理由：漏传会导致保存即丢分包） | T-108 |
| 13 | [api.js#L112-L119](file:///d:/02工作/健身助手/frontend/src/api.js#L112-L119) | `consumePortions(n)` / `restorePortions(items)` | 改为 `consumePortions(n, mealSlot)` / `restorePortions(items, mealSlot)` | T-109 |
| 14 | [TodayView.vue#L100-L118](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L100-L118) | `consumePortions(meals)` 一次性打卡 + `detail → mealsLog` 映射 | 打卡需知道「哪几份属于哪个餐次」，事件需带 `slot` | T-109 |
| 15 | [store.js#L127-L131](file:///d:/02工作/健身助手/frontend/src/store.js#L127-L131) | 旧事件流缺 `slot` 时按 `perSnap` 补占位事件 | 补位事件的 `slot` 置 `null` | T-109 |
| 16 | [CookView.vue#L158](file:///d:/02工作/健身助手/frontend/src/views/CookView.vue#L158) | 分装份数 `Stepper :min="1" :max="10"`（整数） | 若要表达 1.2 份需支持小数步进（分包精度 0.1） | T-108 |
| 17 | [utils.js#L248-L283](file:///d:/02工作/健身助手/frontend/src/utils.js#L248-L283) `evalRules` | 输出「大米 +15g / −15g」的整锅动作 | 改造 md §6.1：改为**按餐次定向调整**，否则会破坏练后餐占全天 40% | T-108（不在本 Sprint 的 T-103/T-104/T-106 范围） |

---

## 六、读码发现：与任务描述不符、或影响实现的地方

### 6.1 `proteinPer` 是既有键，不是新增键

任务描述把 `carbPer` / `proteinPer` / `fatPer` 并列为「settings 新增键」，与实际代码不符：

- `proteinPer` 已在白名单里，语义就是「蛋白 g/kg」—— [settings.js#L8](file:///d:/02工作/健身助手/backend/src/routes/settings.js#L8) `NUMBER_KEYS`
- 默认值 1.5、前端钳制 1.2–2.2、Stepper 步进 0.1 —— [constants.js#L73](file:///d:/02工作/健身助手/frontend/src/constants.js#L73)、[SettingsView.vue#L33](file:///d:/02工作/健身助手/frontend/src/views/SettingsView.vue#L33)、[SettingsView.vue#L158](file:///d:/02工作/健身助手/frontend/src/views/SettingsView.vue#L158)
- 它在 TDEE 模式下参与计算：`p = s.weight * s.proteinPer` —— [utils.js#L30](file:///d:/02工作/健身助手/frontend/src/utils.js#L30)

**处置**：不新增同名键，配额模式**复用**；只补「范围校验」与「null 语义不适用」（该键无 null 态，因为 TDEE 模式必须有值）。契约 §1.2 已按此写。

### 6.2 配额模式强依赖 T-101 的 D3 修复（依赖图需修正）

`calcProfile` 里热量用入参体重、蛋白用 `settings.weight`，两者口径不一致（CALC-AUDIT 的 D3）—— [utils.js#L25-L31](file:///d:/02工作/健身助手/frontend/src/utils.js#L25-L31)：

```js
const w = bodyWeight != null ? bodyWeight : s.weight;
const kcal = tdee - s.gap;        // 用 w（最近体重）
const p = s.weight * s.proteinPer; // 用 s.weight（设置体重）—— 口径分叉
```

配额模式必须用**同一个体重**同时算「三大营养素克数」和「BMI」（BMI 决定是否触发降配）。若 D3 不修，配额模式会继承同一处分叉：70kg 的人最近体重 68kg，BMI 按 68 算、蛋白按 70 算，BMI 分档会与实际取用的体重错位。

**影响**：Sprint 依赖图把 T-101 与 T-102 列为同行并行，但 **T-104 实际同时依赖 T-101**（不只是 T-102）。建议派发 T-104 时显式声明「依赖 T-101 的 D3 已完成」，或让 T-104 直接把 `calcQuotaProfile` 的 `bodyWeight` 作为唯一口径（契约 §2.4 已如此规定，可绕开 D3 的存量影响——但 TDEE 模式一侧仍需 T-101 修）。

### 6.3 `meals` 上限 4 是四处硬编码，跨 T-103 / T-106 / T-109 三个任务

| 位置 | 内容 |
|---|---|
| [backend/src/routes/day-logs.js#L30-L32](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L30-L32) | `if (!Number.isInteger(Number(body.meals)) \|\| Number(body.meals) < 0 \|\| Number(body.meals) > 4) throw 400` |
| [frontend/src/views/TodayView.vue#L21](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L21) | `const MEAL_MAX = 4`（注释写「与 Stepper max、后端 meals 0-4 校验一致」） |
| [TodayView.vue#L168](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L168)、[L247](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L247)、[L294](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L294) | 钳制 / 文案 / Stepper 上限 |

分包后日总份数可达 8（4 个可分餐次 × 2 份），这五处不改会出现「后端 400 拒绝打卡」或「UI 把份数钳到 4」。契约 §4.6 已给出新值 0–8。

### 6.4 `dailyPreview` 的日总量恒为「每份 × 2 + 加项」（T-118 订正）

[utils.js](file:///d:/02工作/健身助手/frontend/src/utils.js)（`dailyPreview` / `perMealIntake` / `potDaysOf`）：`dailyPreview = per × MEALS_PER_DAY(2) + 加项`、`perMealTargets = (profile − ADDONS) / 2`。分包**不改变日总量**，只把这 2 份按各餐比例拆到各餐 —— 示例 A 的 `1.2 / 1.2 / 1.2 / 2.4` → 每天 `0.4 / 0.4 / 0.4 / 0.8`（比例 20/20/20/40），一锅天数 = `⌈份数 ÷ 2⌉`。故 `previewState` 的红黄绿门禁（[store.js](file:///d:/02工作/健身助手/frontend/src/store.js)）与配方库的 `flagBad` **不因分包而变色**，这是刻意的不变量。

> **订正**：本小节原写「分包后各餐份数不等，这两个函数会把预演算错，并直接污染预演红黄绿」——那是 T-117「一锅 = 一天」的旧口径。T-118 已推翻它（见 §8.6）。`dailyPreview` 的签名新增 `allocation` 形参但刻意不参与运算，正是为了把「分包不改日总量」这个不变量钉住。
> **排期结论不变**：T-106 / T-109 仍不应单独上线，但理由已从「预演会算错」变为「分包守恒与按餐次核销必须成套上线，否则 Σ 槽位会与 portions 脱钩」。

### 6.5 `inventory.portions` 已是 REAL，但 UI 步进是整数

`portions REAL NOT NULL`（[db.js#L38](file:///d:/02工作/健身助手/backend/src/db.js#L38)）+ 扣减用 `Math.abs(rest) < 1e-9` 判零（[inventory.js#L106-L107](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L106-L107)）—— 后端**本来就支持小数份**，方案 A 的「1.2 份」不需要改列类型。但做饭页分装 `Stepper :min="1" :max="10"`（[CookView.vue#L158](file:///d:/02工作/健身助手/frontend/src/views/CookView.vue#L158)）与今日页正餐步进都是整数，`packPortions` 也是整数（[store.js#L25](file:///d:/02工作/健身助手/frontend/src/store.js#L25)）。要落 1.2 份，T-108 的 Stepper 必须支持 `step=0.1` 或改用「按目标比例自动分包」的一键计算。

### 6.6 官方材料**没有**给出「早饭兼练前餐」时的碳水百分比

- Excel 第 1–8 张表的碳水克数列是公式单元格，缓存值为 `0`（表头体重未填），**没有可读的官方数值**；
- 视频逐字稿只给四类餐的比例（早饭两成 / 练前两成 / 练后四成 / 其他两成），没有说「一个餐次兼两角时怎么算」。

契约 §2.3 的推导（单位制 + 角色相加 + 其他餐均分）是本 Sprint 的**显式决策**，不是官方原文。推导结果对 7 个 `trainSlot` 全部满足 Σ=100%，且与逐字稿的两处定性描述一致（「午饭和晚饭都是其他餐，碳水比较少」→ 各 10%；夜里练「各餐可以比较均摊，练后餐稍大一点即可」→ 20/20/20/40）。若后续拿到官方数值与此不符，只需替换 `CARB_RATIO` 常量，不动任何函数签名。

### 6.7 `meals_log` 的 `normalizeMealsLog` 会丢字段

[day-logs.js#L59-L63](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L59-L63)：

```js
function normalizeMealsLog(v) {
  if (!Array.isArray(v)) return null;
  const ok = v.filter(e => e && e.per && Number.isFinite(Number(e.per.kcal)));
  return ok.length ? JSON.stringify(ok) : null;
}
```

它保留整个对象（`JSON.stringify(ok)`），所以新增的 `slot` 会被自然保留——**但要补 `slot` 的白名单校验**，否则脏值会静默入库。`rowToLog`（[L65-L83](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L65-L83)）直接透传数组，无需改动。

### 6.8 第 7 种训练时间点的名称不一致

| 来源 | 名称 |
|---|---|
| Excel 表页名（第 7 张减脂表） | 「7减脂-**夜里练**」，表内 `C12` 原文：「夜里练，因此白天要节约饮食配额，夜里练完吃全天最大一顿的练后餐」 |
| Excel 第 17 表《减脂-问答汇总》第 15 问 | 【**晚饭后更晚练**】，附注「各餐可以比较均摊，练后餐稍大一点即可，否则白天会太饿」 |

契约取 `night` + 标签「夜里练」（以表页名为准），并把第 17 问的附注落进 `CARB_RATIO.night` 的取值。

### 6.9 SPEC 与代码的 gap 口径（仅供提示，本契约不负责）

`SPEC.md#L101` 与 `SettingsView.vue#L32` 都写 gap 250–850，一致；而 `减脂方法论改造.md` §三 写「gap 固定 500–900，默认 750」。该不一致由 T-101 处理。**配额模式下 `gap` 被忽略**，所以本契约不受影响，但 T-103 的白名单不要顺手删掉 `gap`（TDEE 模式仍需要）。

---

## 七、冻结声明与下游自检清单

### 7.1 冻结范围

自本文件起，以下五处**不得由单个任务私自变更**，如需变更必须回到 T-102 重新冻结并同步其余任务：

1. §1.2 的 8 个 settings 键名、枚举值、默认值、范围
2. §2.1 的 `QUOTA_TABLE` / `BMI_ADJUST` 常量形状与取值
3. §3.1 的 `mealTargets` 签名与返回结构（含字段名）
4. §4.3 / §4.4 的 `mealAllocation` 字段名（API camelCase `mealAllocation` / DB snake_case `meal_allocation`）与不变量
5. §4.5 的 `consume` / `restore` 请求响应字段名

> **T-121 修正说明（非变更）**：上列第 2 项所指的 `QUOTA_TABLE` 常量形状，在 T-121 做了一次**描述性修正**——从字面量中删除了从未在实现里存在的 `bmiAdjust` 字段（实现见 [frontend/src/constants.js](file:///d:/02工作/健身助手/frontend/src/constants.js) 的 `QUOTA_TABLE`，减脂限定由 [utils.js](file:///d:/02工作/健身助手/frontend/src/utils.js) 的 `st.phase === 'cut' ? pickBmiAdjust(...) : null` 承载）。这是**让契约描述实现、修正错误描述**，不是契约被单方面变更：字段集合、取值、`BMI_ADJUST.cut` 的形状与语义**逐项未动**，也不存在「先改代码加死字段再改契约」的动作。冻结效力不变，仍需变更时照常回到 T-102。

> **T-122 勘误登记（非变更）**：上列第 3 项所指的 `mealTargets` 签名，在 T-122 追加了一个**可选第 4 形参** `carbStage`（取值 `'early' | 'late'`，末期比例表专用；缺省 / 脏值等价于初期，三参调用行为逐字节不变）。这是**调用点全兼容的追加**而非契约被单方面变更：返回结构的字段集合与三参语义**逐项未动**，全部调用点（今日页 / 配方页 / 做饭页）已同步传入 `store.mealStage`；首度引入时做饭页曾漏传（会按初期比例显示末期档），已由 T-123 统一。完整口径见 §8.7。冻结效力不变，仍需变更时照常回到 T-102。

> **T-126 勘误登记（非变更）**：上列第 3 项所指的 `mealTargets` 签名，在 T-126 又追加了一个**可选第 5 形参** `slotsOff`（关闭的餐次 = `settings.mealSlotsOff`；缺省 = 六餐全开，旧调用行为逐字节不变），并把第 1 参 `dayType` 的取值来源由「设置里的静态日类型」改为「**当日生效日类型**」（`pickDayType` 结果，`store.todayDayType`）。这是**调用点全兼容的追加 + 取值口径收敛**而非契约被单方面变更：返回结构的字段集合与三参语义逐项未动；T-126 首度引入时只改到今日页，配方页 / 做饭页的漏传由 T-127 补齐（否则两页会给已关闭的餐次分份数）。完整口径见 §8.8。冻结效力不变，仍需变更时照常回到 T-102。

> **T-129 勘误登记（非变更）**：本契约**未涉及**外食/喝酒。T-129 新增的是一条**叠加在 `mealTargets` 之上的额度修正层**（`utils.adjustForOuting(plan, outing)`），上列第 3 项所指的 `mealTargets` 签名与返回结构**一字未动**（调用方式、字段集合、三参语义全部不变，故 §3.1 / §7.1 的冻结效力不受影响）。完整口径见 §8.9。

### 7.2 下游自检清单

**T-103（后端 settings）**

- [ ] `calcMode` / `phase` / `dayType` / `trainSlot` / `carbStage` 五个枚举键非法值 → 400
- [ ] `carbPer` / `fatPer` 接受 `null`，非 null 时范围校验（1.0–6.0 / 0.3–1.5）
- [ ] `proteinPer` 补 1.2–2.2 范围校验，种子值 1.5 可正常读写
- [ ] 未知键仍 400，消息格式 `未知设置项: <key>` 不变
- [ ] `GET /api/settings` 返回全部新键默认值（三处默认值同步）

**T-104（前端配额引擎）**

- [ ] 减脂期男 70kg、无 BMI 修正、`dayType=train` → `c` 落在 175–210，`p=105`，`f=56`
- [ ] 同用户 `dayType=rest` → `c` 落在 105–140
- [ ] BMI 29 减脂期男 → `per = {carb: 2.5, protein: 1.2, fat: 0.6}`，且 `bmiAdjust.bmiOver === 28`
- [ ] BMI 33 减脂期女 → `per = {carb: 1.7, protein: 1.0, fat: 0.5}`，`bmiAdjust.bmiOver === 32`
- [ ] `QUOTA_TABLE` / `CARB_RATIO` 自检：七条比例序列 Σ = 1
- [ ] `calcProfile` 一行未改，TDEE 模式断言（BMR 1849→2542→1792→135/46/210）不回归

**T-106（后端分包 + 迁移）**

- [ ] 旧库（只有扁平 `items`、无 `meal_allocation` 列）启动 → 自动补列、读取不报错、`recipes.mealAllocation = {}`、`inventory.mealAllocation = null`
- [ ] 新配方带 `mealAllocation` 保存并回读 → 结构一致
- [ ] `mealAllocation` Σ ≠ `portions` → 400
- [ ] `mealAllocation` 含 `snack` 或未知键 → 400
- [ ] `consume({portions:1, mealSlot:'dinner'})` → 只扣 `dinner` 余额与 `portions`，`detail[0].mealSlot === 'dinner'`
- [ ] `consume` 该餐次余额不足 → 回退未分包批次，`detail[].fromUnassigned === true`；`shortage` 语义与现状一致
- [ ] `consume` 不带 `mealSlot` → 行为与今天逐字节一致（回归）
- [ ] `restore` 带 `mealSlot` 后，批次 `meal_allocation[slot]` 与 `portions` 同步回补，Σ 仍 = `portions`

**T-107（mealTargets）**

- [ ] `('train','before_dinner',{c:175,p:105,f:56})` → 晚饭 `roles=['post']`、`c=70`、`p=30`、`f=14`（§3.3 示例）
- [ ] `('train','breakfast_early',...)` → `preSlot === 'breakfast'` 且其 `carbRatio === 0.40`
- [ ] `('none',null,...)` → `preSlot === null && postSlot === null`，四餐比例 0.30/0.30/0.30/0.10
- [ ] 任一情形：`Σ meals[].c === quota.c`（精确到 0.1）、`Σ carbRatio === 1`
- [ ] 练后餐 `p ≥ 30`（当 `quota.p ≥ 30` 时）且 `f ≤ 20`

**T-108 / T-109（UI 与核销）**

- [ ] 配方设定餐次分包 → 保存并重载后结构保留（`api.recipeBody` 已带 `mealAllocation`）
- [ ] 今日页选择餐次核销 → 该餐次余额 −1，当日汇总同步，`mealsLog[].slot` 落库
- [ ] 回撤 → 该餐次余额原路归还，`portions` 与 `meal_allocation` 双双复原
- [ ] `meals` 可达 8 且后端不 400

---

## 八、实现反馈与偏差登记（T-110 收口追加）

> **本节定位**：以下条目是 T-103 ~ T-109 实施过程中，**实现与前面冻结契约不一致、或契约自身存在张力**的地方。
> 原契约正文（第一 ~ 七章）**一字不改**，偏差集中登记在此，便于日后追溯「哪一处是设计、哪一处是妥协」。
> 每条格式：契约原文 → 实现实际 → 取舍理由。冻结声明（§7.1）不因本节而放宽。

### 8.1 `meals` 的小数份：§4.8 的 `"meals": 4.8` 已跑通（T-111 收口）

- **契约原文**：§4.8 第 4 步示例给出 `{ "meals": 4.8, "consumed": 4.8, ... }`。
- **首次实现实际（T-110 登记）**：[day-logs.js](file:///d:/02工作/健身助手/backend/src/routes/day-logs.js#L28-L33) 的 `assertLogBody` 强制 `Number.isInteger(meals)`（0–8），`4.8` 被 400 拒收；当时以「整数核销 + 小数余量留在批次 `meal_allocation` 里累计」妥协。
- **现实现（T-111）**：`meals` 放开为 **0–8 且最多 1 位小数**（乘 10 判整 + `1e-9` 容差，非负与上限约束不变），落库前收到 0.1 网格；`day_logs.meals` 列类型由 `INTEGER` 改为 `REAL`（SQLite 的整数亲和性本就能存 4.8，**旧库无需迁移**）。今日页与记录页同步支持 0.1 粒度：份数步进 `step=0.1`、`spreadPlan` 让「Σ 各餐分配 = 本次核销总份数」严格成立（末位有权重的槽位吃余数）、`fmtQty` 按 1 位小数展示且整数不带 `.0`。
- **口径变更的依据**：用户的真实用法是「做好一锅放冰箱，每餐吃多少盛多少」——「份」不是物理饭盒，而是从锅里盛出的量，1.2 / 2.4 都是真实吃量。原妥协会让各餐完成度长期差 0.2 而对不齐；历史打卡数据量极小，用户已确认不做迁移。
- **连带改动**：`db.js` 的列类型与注释；`inventory.js` 的扣减余量容差（`left <= 1e-9`，避免跨批次小数扣减报出 `2.2e-16` 的假 shortage 与近零幽灵事件）与**读路径 0.1 收整**（`buildBatch` 的 `portions` / `mealAllocation`），保证只读页面也不会显示 `3.5999999999999996 式`长尾；`store.js` 的旧事件占位补齐改为按「Σ事件份数」而非事件条数度量缺口；`TodayView.vue` / `LogView.vue` / `utils.js` 的份数展示。
- **不受影响**：`SPEC` 与 `calcStreak` 的「打卡率 = `meals > 0`」口径（小数与整数同为正值）；`backup.js` 的导入导出原样透传 `meals`，`resolveCheckedIn` 的 `Number(meals) > 0` 推断对小数同样成立。

### 8.2 `meals_log` 事件新增 `portions` 字段（契约只定义了 `slot`）

- **契约原文**：§4.6 只规定 `meals_log` 事件新增 `slot`，事件仍是「一份一条」。
- **实现实际**：后端 [inventory.js](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L100-L109) 的 `pushDetail` 会把一笔小数份核销拆成 `ceil(take)` 条事件（每条营养按实际份数折算），因此「事件条数 ≠ 真实份数」。前端在 [TodayView.vue](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L242-L254) 用 `per.kcal ÷ 批次每份 kcal` 还原真实份数，并写入新增的 `portions` 字段。
- **取舍**：每条事件附带 `portions`；回撤按 `Σ portions` 原路归还，而不是按事件条数。
- **理由**：按条数计数会在小数份核销时多还/少还，直接破坏 §4.4 的 `Σ = portions` 不变量。

### 8.3 UI 刻意不走 `mealSlot: null` 的兼容路径

- **契约原文**：§4.5 规则 1 为向后兼容保留了「`mealSlot` 缺省 / `null` → 完全走现有 FIFO」的路径，且只改 `portions`。
- **实现实际**：后端该路径（[inventory.js](file:///d:/02工作/健身助手/backend/src/routes/inventory.js#L114-L127) `consumeFifo`）只减 `portions`、绝不触碰 `meal_allocation`，会让**已分包批次**出现 `Σ槽位 ≠ portions` 的永久脱钩，与 §4.3 / §4.4 的不变量冲突且不可逆。
- **取舍**：UI（[TodayView.vue](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue#L230-L240) `consumeByPlan`）每一笔核销都显式携带具体 `mealSlot`，从不走 null 路径；`api.consumePortions(n, mealSlot)` 仍允许 `null` 仅为老客户端兼容，前端不会这么调。
- **理由**：§4.5 的 null 分支是「向后兼容旧客户端」的逃生口，不是给新 UI 用的日常路径——新 UI 用它会自毁分包守恒。

### 8.4 §3.4 示例 B 自相矛盾：练后餐硬约束 vs 「末项吃余数」

- **契约原文**：§3.2 第 2/3 步规定「前 N−1 项 `round1`，末项吃余数」；而 §3.4 示例 B 中练后餐（`post`）恰好是当日**末项**。照原文复算：
  - 脂肪 `f_each = 22.5 > 20` → 练后餐先压到 20，再由末项吃余数 `90 − 69.9 = 20.1`，**又冲破 ≤20**；
  - 蛋白同理受余数挤压（落到 33.6 一类样本值）。
- **冲突点**：§3.2 与 §7.2 又同时要求练后餐满足 `p ∈ [30,50]`、`f ≤ 20` 的**硬约束**。在「练后餐 = 末项」时两者无法同时满足。
- **取舍**：**硬约束优先**。[utils.js](file:///d:/02工作/健身助手/frontend/src/utils.js#L153-L161) 的 `roundSeries(values, total, keepIdx)` 让 `keepIdx`（练后餐）不参与吃余数，余数改由「最后一个非练后餐」吸收。
- **后果**：§3.4 示例已于 2026-09 **按本取舍订正**（原 `f: 20.1 / p: 33.7` 改为 `f: 20.0 / p: 33.8`，余数改由 `dinner` 吸收），订正后的数值由实现实跑取得、与实现逐字段一致；§3.3（练后餐非末项）与 §3.5（无练后餐）也精确复现。该取舍已成为回归网——`verify/meal-audit.mjs` [C] 段对 §3.4 目前仍留 ±0.2 容差（值既已确定，容差可后续收紧）。

### 8.5 已确认对齐、无需偏差登记的项（T-110 核对）

- 系数区间三处一致：`proteinPer` 1.2–2.2（后端 `PER_RANGES` / 前端 Stepper / 计算层 `perOverride`）、`carbPer` 1.0–6.0、`fatPer` 0.3–1.5。
- §1.7 的 7 个新键默认值在 [seed.js](file:///d:/02工作/健身助手/backend/src/seed.js#L52-L61) `DEFAULT_SETTINGS`、[db.js](file:///d:/02工作/健身助手/backend/src/db.js#L126-L133) `ensureSettingsKeys`、[constants.js](file:///d:/02工作/健身助手/frontend/src/constants.js#L70-L79) `SETTINGS_FALLBACK` 三处**逐字一致**。
- `gap` 钳制 250–850：前端 `clampSettings` 与 SPEC v2.1 一致（属 SPEC 行为变更，台账已同步至 `CALC-AUDIT.md`）。
- **`Σ槽位 = portions` 的约束边界**：该不变量只在「写入时（`normalizeMealAllocation`）」与「按餐次核销 / 回补路径（`mealSlot` 非空）」维护；不带 `mealSlot` 的 FIFO 路径按契约原文会让两者脱钩，属**已知且刻意**的向后兼容行为（见 8.3）。

### 8.6 「Σ 各餐份数 × 每份营养」旧口径被推翻（T-118 收口）

- **旧口径（T-117 引入）**：把分包理解为「这一锅 = 一天」，`dailyPreview` 的日总量改为 `Σ (每份营养 × 各餐份数)` —— 一锅 6 份、分包 `1.2 / 1.2 / 1.2 / 2.4` 时，当天就算 6 份。
- **为什么被推翻**：用户的真实用法是**做好一锅放冰箱、每餐吃多少盛多少，一锅通常吃好几天**。分包表达的是「这一锅的各餐**比例**」，不是「一天吃多少」。旧口径下，`autoGenerate` 按 `days × 2` 生成份数（6 份的锅本就是 3 天的料），用户一标分包，当天预演就从 2 份变成 6 份 —— 门禁直接 **+120% 爆红**，红黄绿失去意义。
- **现口径（正确）**：日总量**恒为** `每份营养 × MEALS_PER_DAY(2) + 加项`，与是否分包无关；分包只按比例把这 2 份拆到各餐（一锅 6 份、分包 `1.2 / 1.2 / 1.2 / 2.4` → 每天 `0.4 / 0.4 / 0.4 / 0.8`，比例 `20 / 20 / 20 / 40`）。一锅天数 = `⌈份数 ÷ 2⌉`，**分包不改变天数**。实现落在 `dailyPreview`（`allocation` 形参刻意不参与运算）、`perMealIntake`、`potDaysOf`。
- **未被推翻的部分**：`Σ mealAllocation = portions` 这个**数据层不变量本身仍然成立** —— 后端写入校验（`normalizeMealAllocation`）与按餐次核销 / 回补路径都在守它（§4.3 / §4.4 / 8.5）。被推翻的只是「拿 Σ 各餐份数当一天的量」这个**推论**，不是这个不变量。
- **连带订正**：§5 影响清单第 4 项与 §6.4 的旧表述已就地订正；`verify/calc-audit.mjs` 新增 `[L]` 段，专门守住「同一配方在分包与未分包两种状态下 `dailyPreview` 日总量逐值相等、红黄绿 `worst` 一致」。

### 8.7 `mealTargets` 追加可选第 4 形参 `carbStage` + 引入 `CARB_RATIO_LATE`（T-122 收口）

- **契约原文**：§3.1 / §7.1 把 `mealTargets(dayType, trainSlot, quota)` 的**三参签名**与返回结构列为冻结内容；§2.3 的比例表只有一张 `CARB_RATIO`（初期口径 2:2:4:2）。
- **实现实际**：签名扩为 `mealTargets(dayType, trainSlot, quota, carbStage?)`，`carbStage ∈ {'early','late'}`，缺省 / 脏值按初期。仅当 `carbStage === 'late'` **且** `dayType === 'train'` 时改查新增常量 `CARB_RATIO_LATE`（[constants.js](file:///d:/02工作/健身助手/frontend/src/constants.js)）：沿用同一套 10 单位制表达末期口述比例 —— **早饭 3 / 练前 2 / 练后 5 / 其他餐 0**，即早饭 30%、练前 20%、练后 50%、其他餐 0（`breakfast_early` 早饭兼练前 = 3+2 = 5 单位 = 50%）。休息日 / 无训练是「四餐均摊」结构，其他餐就是正餐本身，无「其他餐」可收敛，故不受该参影响。三处调用点（今日页 / 配方页 / 做饭页）统一传入 `store.mealStage`（`phase === 'cut' && carbStage === 'late'` 才为 `'late'`，增肌期即便残留 `late` 也按初期表）。
- **`CARB_RATIO_LATE` 的引入与「0 比例餐次仍保留蛋白/脂肪目标」**：末期表里「其他餐」碳水比例为 0，但 **0 比例只作用于碳水** —— 这些餐次仍按 §3.2 第 2/3 步拿到蛋白质与脂肪目标（均分，或按练后餐约束取值），不会被整餐抹掉。另一处连带：§3.2 的「末项吃余数」在末期下会把 ±0.1g 余数落到比例为 0 的餐次（产出非零甚至负值），故 `roundSeries` 收紧为「余数只落**比例为正**的餐次」——初期表全为正，此处与原口径逐值等价。常量自检仍守每条比例序列 Σ = 1（`quota-audit`）。
- **兼容性（为何算追加而非破坏）**：三参调用行为**逐字节不变**（缺省即初期表）；`verify/meal-audit.mjs` 已加断言「缺省 / 脏 `carbStage` 与初期逐字段一致」「`rest` / `none` 传 `late` 仍走均摊」。返回结构的字段集合与三参语义未动，故 §3.1 冻结效力不变，仍需变更时照常回到 T-102。
- **唯一无明文口径处的取舍（`night` 末期）**：契约 §2.3 规则 4 与官方第 17 问只给了**初期**下的 `night` 处理；末期 `night`（夜里练）没有独立练前餐，那缺位的 2 单位**并入练后餐**（练后 5+2 = 7 单位 = 70%，早饭稳定保持 3 单位 = 30%），而非并进早饭。**依据**：官方第 7 表页原文「夜里练……夜里练完吃全天最大一顿的练后餐」，且与 §2.3 规则 4「保持练后餐最大、不并入早饭」是同款例外。
- **连带改动**：做饭页 `CookView.vue` 的 `mealTargets` 调用点曾在 T-122 首度引入时漏传 `carbStage`（末期档下仍按初期比例显示与一键分配，与今日页 / 配方页不一致），已由 T-123 补传 `store.mealStage`；全仓其余调用点（今日页 / 配方页）本已传参，`verify/` 下刻意不传的断言用于守「缺省 = 初期」的兼容性。

### 8.8 `mealTargets` 新增可选第 5 形参 `slotsOff` + 第 1 参改传「当日生效日类型」（T-126 引入 / T-127 收口）

- **契约原文**：§3.1 / §7.1 把 `mealTargets(dayType, trainSlot, quota)` 的三参签名与返回结构列为冻结内容；§1.2 的 settings 键表里没有「关闭的餐次」；§2.3 的比例表只按日类型取，与「用户当天关掉某一餐」无关。
- **实现实际（T-126）**：
  - 签名扩为 `mealTargets(dayType, trainSlot, quota, carbStage?, slotsOff?)`，`slotsOff` = `settings.mealSlotsOff`（六餐名数组 `breakfast/lunch/pre/post/dinner/snack`，去重后不可全关）。命中的餐次从当天结构**整体移除**，其余餐次的碳水比例按比例**归一化**（÷ 剩余比例之和，故 `Σ carbRatio` 仍 = 1；蛋白与脂肪本就按餐次数均分，少一餐即按少一餐的总数重分，各餐三项相加仍 = 全天配额）。三种脏输入（非数组 / 过滤后一餐不剩 / 剩余比例之和为 0 —— 末期表里「其他餐」比例为 0，只留它们等于没有碳水可分）一律退回原结构：宁可忽略这个设置，也不能让分餐表变成空数组或 NaN。缺省 = 六餐全开，老调用点行为**逐字节不变**。
  - 第 1 参 `dayType` 的**取值来源变了**：由「`settings.dayType`（静态设置）」改为「**当日生效日类型**」= 当日登记值（`day_logs.day_type`）→ `settings.dayType`（降级为「默认日类型」）→ `'none'`，由 `utils.pickDayType` 给出（`store.todayDayType`）。参数名与语义（三个枚举）未变，变的是调用点传什么。
- **T-127 收口**：T-126 首度引入时只改到今日页与设置页，**配方页 / 做饭页仍传 `store.settings.dayType` 且不传 `mealSlotsOff`** —— 后果是这两页的分餐比例与今日页不一致（切了登记日类型后仍显示静态默认日类型的比例），且会给**已关闭的餐次**分到份数、并把这些份数写进配方分包 / 批次 `mealAllocation`。现两页统一改传 `store.todayDayType` + `store.settings.mealSlotsOff`，三页同源。
- **连带的口径变化（今日页）**：`settings.mealSlotsOff` 含 `snack` 时，今日页的**「晚加餐」整张卡隐藏**（不是留空壳）—— 「零食/夜宵」在口径上就是那 10% 机动加餐额度，与这张卡是同一个东西；早餐是独立餐次、不在 `snack` 内，卡不动。设置页餐次开关旁同步加一句说明。
- **连带改动（后端餐次枚举去重，T-127）**：`mealSlotsOff` 的合法值域（六餐全集）原先在 [settings.js](file:///d:/02工作/健身助手/backend/src/routes/settings.js) 本地另抄一份 `MEAL_SLOTS`，现改为 [helpers.js](file:///d:/02工作/健身助手/backend/src/helpers.js) 单点导出、路由引用同一份；`PACK_SLOTS` 语义不变（仍是去掉 `snack` 的 5 席分包槽位，两者关系已在源码注释写清）。`meal-slots-audit` 新增源码级断言守「定义处唯一 + 校验处引用同一份」。
- **兼容性（为何算追加而非破坏）**：`carbStage` / `slotsOff` 都是**可选尾参**，三参调用行为逐字节不变；`dayType` 的取值变化只发生在调用点，`mealTargets` 自身的收敛逻辑（非法值退回无训练行）未动。返回结构的字段集合与三参语义未动，故 §3.1 冻结效力不变，仍需变更时照常回到 T-102。

### 8.9 外食/喝酒的记录与随餐修正（T-129 · 本契约之外的新增层）

- **契约原文**：本契约（T-102 冻结版）全文未涉及「外食 / 喝酒」。与它最近的两条是 §1.2 的 settings 键表（无相关键）与 §2.3 / §3.2 的分配算法（配额如何拆到各餐）。
- **实现实际（T-129）**：
  - **新增数据**：`day_logs.outing TEXT`（`NULL` = 未登记），形状 `{type,level,baijiu,beer,slot}`。类型 ∈ `eat`/`drink`/`both`，外食量级 ∈ `light`/`normal`/`big`，酒量 `baijiu`（两，0–50）/ `beer`（瓶，0–30），餐次 ∈ 六餐全集（缺省 `dinner`）。幂等迁移 `ALTER TABLE day_logs ADD COLUMN outing TEXT`（不回填，整列 `NULL` = 全部未登记，与改造前逐字段等价）；`PUT /api/day-logs/:date` 接受并回读；`BACKUP_COLUMNS.day_logs` 已补 `outing`（导出为与 GET 同形的对象、导入兼容对象/文本/缺键三种来源）。
  - **折算口径**（官方 Excel 第 17 表问答 13）：**1 两白酒（50g）≈ 1 瓶啤酒（600g）≈ 200 kcal ≈ 50g 碳水**，酒**整块记作碳水**、蛋白与脂肪记 0（他的执行口径是「先算出每周酒精摄入，再从饮食里扣」）。外食按**粗档位估算**：轻量 375 kcal（c40/p20/f15）、一顿普通外食 530 kcal（c55/p28/f22）、大餐 840 kcal（c85/p35/f40）；热量由 `4c+4p+9f` 派生，保证界面读数自洽。口径在 UI 上显式标为「估算」。
  - **新增分配层** `utils.adjustForOuting(plan, outing)`：**叠加在 `mealTargets` 之上**，`mealTargets` 的签名（含 T-122 / T-126 的两个可选尾参）与返回结构**一字未动**。算法：以记录里的 `slot` 为锚点取**前一餐 + 后一餐**，对折算量的每个宏量按 `c → p → f` 顺序在两餐之间**按各自该类目标的占比分摊**（碳水优先扣），每餐扣到 0 即停、绝不出现负数；锚点不在当天结构里（登记后改了日类型或关掉了那一餐）时退回「当天最后两餐」。返回 `outing` 元信息（`need` / `adjusted` / `cuts` / `over` / `overKcal`）供界面展示「哪一餐被压低了、压了多少」。
  - **硬不变量**：前后两餐扣得动时 **`Σ 各餐 = 全天配额 − 外食占用` 逐宏量严格成立**；扣不动（含外食折算量本身超过全天配额）时差额记入 `over`，界面必须明示「今天已超 X kcal」并说明前后两餐已压到 0，**不得静默显示绿色**。未登记 / 脏值 / 删除记录时 `adjustForOuting` 逐字段等价于原结构（各餐目标完全回落）。
  - **计入当日摄入**：`utils.dayIntake` 增加 outing 一项，今日环、各餐完成度归集、记录页回溯与达标天数随之变化。
- **为什么落在这里**：它不改配额表、不改分餐比例表、不改 `mealTargets`，只是**在既定结构上做加减**（与用户方法论一致：「各餐碳水比例维持既定结构，外食修正不破坏它」）。故本契约的冻结内容（§7.1 五处）一项未变，此处仅作为**契约外新增层的实现登记**留痕，回归网由 `verify/outing-audit.mjs`（100 项）承担。
