# 配方计算监控专题（CALC-AUDIT）

> 生成：2026-09-17 · 数据源：NAS 实况 `http://192.168.31.8:19881/api`（非演示数据）
> 目的：把「配方计算」全链路钉死，并解释「执行下来吃得很饱、不像减脂」的真实原因
> 复跑：`node verify/calc-audit.mjs`（改动配方或计算层代码后重跑，全链断言）
> 复审：2026-09-18（T-115）—— 缺陷状态、NAS 表期望值、附录断言数逐条复核，结论与新口径登记见 §5.1

---

## 0. 结论速览

1. **计算层正确** —— 后端 `calcRecipeTotals`、前端 `calcTotals`、手工独立核算三方逐项一致，未发现数值错误。BMR/TDEE/目标/宏量也全部对上 SPEC 断言。
2. **「吃得很饱」是份量结构问题，不是算错** —— 每份 805 kcal / 生重 600 g（熟重近 800 g），而系统把全天 1799 kcal 只切成 **2 餐**，每餐 900 kcal。
3. **「不饿」的真正原因是加餐没进预算，且存在一次口径断链** —— 09-17 库存扣减 4 份但只记录 2 份，**1396 kcal 未进入任何统计**。
4. **台账缺陷已按 T-101 处理完** —— D1（未勾油也加油）、D2（多主食碳水超配）、D3（蛋白/热量双体重口径分叉）已修，D5（查不到 `oil` 时抛错）与 D1 同根因、一并解决，D4 已于 v2.4 消除。同时按 SPEC 把前端 `gap` 钳制由 **500–900 放宽为 250–850**（覆盖 0.25/0.5/0.75 kg/周 → 275/550/825 kcal），属行为变更。
5. **「人不自觉多摄入 20%」理论成立，但不能直接砍目标 20%** —— 理由与替代做法见第 6 节。

---

## 1. 对账方法

三方独立计算同一输入，比对结果：

| 来源 | 实现位置 |
|---|---|
| 后端 | [helpers.js](file:///d:/02工作/健身助手/backend/src/helpers.js#L23-L46) `foodNutrition` / `calcRecipeTotals` |
| 前端 | [utils.js](file:///d:/02工作/健身助手/frontend/src/utils.js#L228-L243) `calcTotals` / `perOf` |
| 手工 | 按「每100g值 × 克重 ÷ 100」逐项累加，无程序参与 |

---

## 2. 计算链路逐环节复核

### 2.1 参数链（实况：`weightTrack=true`，最新体重 89 kg）

实况 settings：`weight 90, height 175, age 27, sex m, act 1.375, gap 750, proteinPer 1.5, fatRatio 23, manualTdee null`

| 环节 | 公式 | 精确值 | 系统输出 |
|---|---|---|---|
| BMR | 10×89 + 6.25×175 − 5×27 + 5 | 1853.75 | 1854 |
| TDEE | BMR × 1.375 | 2548.91 | 2549 |
| 目标热量 | TDEE − gap(750) | 1798.91 | 1799 |
| 蛋白 P | 89 × 1.5（体重口径统一走最近体重 89） | 133.50 | 134 |
| 脂肪 F | kcal × 23% ÷ 9 | 45.97 | 46 |
| 碳水 C | (kcal − 4P − 9F) ÷ 4，取 P = 133.5 | 212.80 | 213 |

> **T-101 修 D3 同步**：P/C 的期望值由修复前的 `135 / 211` 校正为 **`134 / 213`**。原实现热量用最新体重 89、蛋白仍乘静态 `settings.weight` 90，双口径分叉；修复后 `calcProfile` 的 P 与热量统一乘传入体重，`89 × 1.5 = 133.5 → 134`、碳水反推 `212.80 → 213`。BMR/TDEE/目标（1854/2549/1799）与 SPEC 基准不受影响。

**SPEC 基准校验**（age 30 / weight 90 / 不启用体重追踪）：BMR 1849 → TDEE 2542 → 目标 1792 → 135 / 210 / 46，与 [SPEC.md](file:///d:/02工作/健身助手/SPEC.md#L259) 断言完全一致。✅

### 2.2 一锅总量

**当前配方「虾仁炒饭」**（portions 6）：`rice 600, mushroom 300, onion 300, carrot 300, corn 540, egg 600, shrimp 900, oil 60`

| 食材 | 克重 | 热量 | 蛋白 | 碳水 | 脂肪 |
|---|---|---|---|---|---|
| 大米 | 600 | 2076.0 | 44.4 | 467.4 | 4.8 |
| 香菇 | 300 | 78.0 | 6.6 | 15.6 | 0.9 |
| 洋葱 | 300 | 120.0 | 3.3 | 27.0 | 0.6 |
| 胡萝卜 | 300 | 117.0 | 3.0 | 26.4 | 0.6 |
| 玉米粒 | 540 | 604.8 | 21.6 | 123.1 | 6.5 |
| 鸡蛋 | 600 | 864.0 | 79.8 | 16.8 | 52.8 |
| 虾仁 | 900 | 432.0 | 93.6 | 7.2 | 6.3 |
| 食用油 | 60 | 539.4 | 0.0 | 0.0 | 59.9 |
| **合计** | **3600** | **4831.2** | **252.3** | **683.5** | **132.4** |

系统 `recipes.totals` 返回 `4831.2 / 252.3 / 683.52 / 132.42` —— **完全一致** ✅

**默认「一锅出」基准**（`rice 510, pork_loin 500, broccoli 400, carrot 400, corn 300, oil 60`）：
手工 `3715.0 / 171.14 / 521.09 / 110.32`，SPEC 断言 `3715 / 171 / 521 / 110` —— **一致** ✅

### 2.3 每份与每日预演

| 指标 | 虾仁炒饭 | 默认一锅出 |
|---|---|---|
| 每份 kcal | 805.2 | 619.17 |
| 每份 P/C/F | 42.05 / 113.92 / 22.07 | 28.52 / 86.85 / 18.39 |
| 每日预演（2份+加项 229.8） | **1840.2 / 132.1 / 232.04 / 47.14** | 1468.13 / 105.05 / 177.90 / 39.77 |
| vs 目标 1799/134/213/46 | +2.3% / −1.4% / +8.9% / +2.5%（**全绿**） | −18.4% / −21.6% / −16.5% / −13.5% |
| 理论缺口 | 2549 − 1840.2 = **708.8** | 2549 − 1468.13 = 1080.8 |

> 注意碳水 +8.9% 已贴近 10% 的绿/黄分界线，任何加料都会翻黄。（目标口径已按 T-101 修 D3 同步为 1799/134/213/46）

---

## 3. 实际执行核算（真实打卡）

### 3.1 09-16

| 项 | 明细 | kcal | P | C | F |
|---|---|---|---|---|---|
| 正餐 | 2 份 × 698 | 1396.0 | 59.6 | 188.0 | 47.0 |
| 蛋白粉 | 2 勺 | 229.8 | 48.0 | 4.2 | 3.0 |
| 早餐 | 红薯 230 g | 140.3 | 2.5 | 35.2 | 0.5 |
| 晚加餐 | — | 0 | 0 | 0 | 0 |
| **合计** | | **1766.1** | **110.1** | **227.4** | **50.5** |
| vs 目标 1799/135/211/46 | | −1.8% 绿 | −18.4% 黄 | +7.6% 绿 | +9.8% 绿 |

### 3.2 09-17

| 项 | 明细 | kcal | P | C | F |
|---|---|---|---|---|---|
| 正餐 | 2 份 × 698.23 | 1396.5 | 59.6 | 187.9 | 47.1 |
| 蛋白粉 | 0 勺 | 0 | 0 | 0 | 0 |
| 早餐 | 鸡蛋 301 g（≈6 个）+ 玉米 201 g | 658.6 | 48.1 | 54.3 | 28.9 |
| 晚加餐 | 红薯 200 g | 122.0 | 2.2 | 30.6 | 0.4 |
| **合计** | | **2177.0** | **109.9** | **272.8** | **76.4** |
| vs 目标 1799/135/211/46 | | **+21.0% 红** | −18.6% 黄 | **+29.1% 红** | **+66.2% 红** |

**单是早餐的 6 个鸡蛋就贡献了 26.5 g 脂肪**，占全天 76.4 g 的三分之一。

### 3.3 关键异常：库存扣减 4 份，记录只有 2 份

09-17 的 day_logs 里：`meals = 2`，但 `consumed = 4`，且 `mealsLog` 只剩 2 条事件。

差异 **1396 kcal**（4−2 = 2 份 × 698）**未进入任何当日统计**。成因是 [TodayView.vue](file:///d:/02工作/健身助手/frontend/src/views/TodayView.vue) 的 `setMeals` 下调语义（该函数及下方代码片段已随 v2.4 重构移除，仅作历史记录）：

```js
toast('下调仅改记录，已扣库存不自动回补');
store.today.meals = v;
store.today.mealsLog.splice(v);   // 事件流被裁掉，但库存已实际扣减
```

库存（实物真相）与 `meals`（记录真相）在此分叉。09-17 记录 2177 kcal，**若按实物口径（4 份正餐）应为 3573 kcal**。

> 这一条比任何理论都重要：**先修口径，再谈目标**。

> **更新（v2.4 已修复）**：该分叉已从结构上消除——今日页打卡改为「一次性确认 + 回撤」，份数与库存只在打卡这一个动作里同时变化；`setMeals` 不再裁剪事件流，原「下调仅改记录、不回补库存」的语义已废弃。老库按 `meals > 0` 回填为已打卡，库存扣空的批次改为置 0 保留以支持回撤归还。

---

## 4. 偏差归因

### 4.1 份量体积过大 —— 「吃得很饱」的直接来源

虾仁炒饭每份生重 = 3600 ÷ 6 = **600 g**，煮制吸水后熟重约 **750–800 g**：

| 组成 | 每份生重 | 换算 |
|---|---|---|
| 大米 | 100 g | 熟饭约 230–250 g（一大碗） |
| 虾仁 | 150 g | — |
| 鸡蛋 | 100 g | 2 个 |
| 玉米粒 | 90 g | — |
| 香菇/洋葱/胡萝卜 | 150 g | — |
| 食用油 | 10 g | 约 1 瓷勺 |

能量密度仅 **1.34 kcal/g**（普通家常菜约 1.8–2.5）。**体积大、热量低 → 必然饱**。这是高体积减脂餐的正常特征，不是缺陷。

### 4.2 餐次结构：1799 kcal 只分 2 餐

[dailyPreview](file:///d:/02工作/健身助手/frontend/src/utils.js#L314-L322) 把每日固定拆成 `per × 2`，即每餐 900 kcal（含蛋白粉）。常规减脂方案把 1800 kcal 分成 3–4 餐，每餐 450–600 kcal。**把 900 kcal 压进一餐，饱腹感自然超出预期。**

> **T-119 订正**（原 T-115 复核注「`dailyPreview` 口径未变」已失真）：`dailyPreview` 在 T-117 曾被改为「一锅 = 一天」（日总量 = `Σ 各餐份数 × 每份营养`），随后被 **T-118 推翻** —— **现口径：日总量恒为「每份 × `MEALS_PER_DAY`(2) + 加项」，与是否分包无关；分包只按各餐比例把这 2 份拆到各餐**（`perMealIntake`），一锅天数 = `⌈份数 ÷ 2⌉`（`potDaysOf`），分包不改变天数。函数签名新增 `allocation` 形参但刻意不参与运算；`verify/calc-audit.mjs` 的 `[L]` 段守住「分包 / 未分包两种状态下日总量逐值相等、红黄绿 `worst` 一致」。本节的「只分 2 餐」诊断仍成立但已不再无解 —— T-108/T-109 引入了 `mealTargets` 各餐目标与餐次分包，今日页按餐展示碳蛋脂目标与完成度，不再只有「2 顿均分」一种视图。

### 4.3 加餐是黑箱 —— 「不饿」的真凶

| 口径 | 覆盖范围 | 09-17 结果 |
|---|---|---|
| 配方页「每日预演」 | 仅 2 份正餐 + 蛋白粉 | 1840 kcal，**全绿** |
| 今日页实际记录 | 正餐 + 蛋白粉 + 早餐 + 晚加餐 | 2177 kcal，**超标 21%** |

配方页的红黄绿**不包含早餐/晚加餐**，而今日页的加餐 chips 可以无限点（单项上限 2000 g），全程没有任何红黄绿拦截。**「预演绿」≠「当天绿」。**

### 4.4 口径漂移与单位陷阱

| 项 | 现状 | 影响 |
|---|---|---|
| 每份快照 | `perSnap` 是旧「一锅出」698 kcal，当前配方为「虾仁炒饭」805 kcal | 差 107 kcal/份，库存空后核销会按 805 估算，口径跳变 |
| 虾仁 900 g | 若按**带冰衣**重量记，实际虾肉仅约 630–720 g | 高估 20–30% |
| 鸡蛋 600 g | 按**带壳**称 vs 去壳蛋液，差约 10% | 6 个带壳蛋 ≈ 300 g，蛋液仅约 255 g |
| 主食生熟 | 全部按**干重/生重**（大米干重 600 g → 熟饭 1400 g） | 若按熟重称量会系统性低估 |

---

## 5. 缺陷台账（T-101 处理结果）

| # | 位置 | 问题 | 影响面 |
|---|---|---|---|
| D1 | [utils.js L321-L328](file:///d:/02工作/健身助手/frontend/src/utils.js#L321-L328) | `allocFat` 中 `ids.find(...) \|\| 'oil'` 兜底：**未勾选「油」时仍会强行加入 5–15 g 油**。**已修（T-101）**：改为只在用户确实勾选了 `oil`/`sesame_oil` 且食材库存在该 id 时才补，未勾选时结果中不再出现 `oil` 键 | 每份凭空 +45~135 kcal |
| D2 | [utils.js L298-L311](file:///d:/02工作/健身助手/frontend/src/utils.js#L298-L311) | `allocGrain` 每个主食都用同一个 `cLeft` 计算、互不扣减，仅靠 `cap` 兜底。**已修（T-101）**：每分配一种主食即扣减剩余碳水（按剩余主食数均分 + `fitGrainCarb` 退档），多主食不再重复占用同一缺口 | 实测复现：`rice + brown_rice` 每份碳水 110.24 g > 每餐目标 102.90 g（低密度主食因 cap 截断反而不会触发） |
| D3 | [utils.js L27-L42](file:///d:/02工作/健身助手/frontend/src/utils.js#L27-L42) | `calcProfile` 热量用最新体重（89），蛋白仍用 `settings.weight`（90）。**已修（T-101）**：P 与热量统一乘同一个体重口径 `w`（传入 `bodyWeight`，缺省退回 `settings.weight`），NAS 实况由 135/211 校正为 134/213 | 口径轻微不一致 |
| D4 | [utils.js L57-L62](file:///d:/02工作/健身助手/frontend/src/utils.js#L57-L62) | ~~预演固定 2 份，核销允许 0–4 份；下调不回补库存、事件流被裁~~ **已于 v2.4 消除**：打卡改一次性确认 + 回撤，份数与库存只在打卡动作里同时变化 | 预演仍固定按 2 份计（属设计口径，非缺陷）；分叉问题已解决 |
| D5 | [utils.js L321-L328](file:///d:/02工作/健身助手/frontend/src/utils.js#L321-L328) | 与 D1 同根因：食材库中若查不到 `oil`，`allocFat` 兜底后无条件 `addPerFood` → `TypeError`。**已修（T-101）**：`allocFat` 先查库再 `addPerFood`，查不到即不分配，不再抛错 | 自动搭配直接抛错中断（线上 seed 恒有 oil，故未暴露） |

### 5.1 复审登记（T-115 · 2026-09-18）

**逐条复核结论**（方式：读实现 + 重跑 `node verify/calc-audit.mjs`）：

| 复核对象 | 结论 |
|---|---|
| D1 / D2 / D3 / D5 缺陷状态 | **仍然准确**，四条均已在代码中修复且未被后续任务回退：`allocFat` 仍只在用户勾选 `oil`/`sesame_oil` 且库中存在时补油；`allocGrain` 每分配一种主食即扣减剩余碳水；`calcProfile` 的 P 与热量统一乘入参体重 `w`；`allocFat` 先查库再 `addPerFood` |
| D4 | **仍然准确**（v2.4 已结构性消除 consumed/meals 分叉；T-111 起份数可为 0.1 小数，本条结论不变） |
| §2.1 NAS 表期望值 | **无需变更**。1854 / 2549 / 1799 / 134 / 213 / 46 与 `verify/calc-audit.mjs` 的断言逐项一致（该脚本即为此表而写） |
| §2.2 一锅总量 · §2.3 每份与预演 | **无需变更**。4831.2·252.3·683.52·132.42、3715/171.14/521.09/110.32、1468.14、缺口 1074 仍全部由脚本逐项断言通过 |
| 附录断言数 | **T-119 更新**：`calc-audit` 当时为 **69 项**（T-115 时为 44 项，其后 T-116 ~ T-118 陆续新增断言，见 §5.2 R6），最近一次运行 69 项通过 / 0 项失败。**T-127 实跑已增至 89 项通过 / 0 项失败**（退出码 0，见 §5.3） |
| `gap` 钳制 250–850 | **仍然准确**（前端 `clampSettings` 与 `SettingsView` 的 `min/max` 一致，SPEC v2.6 亦为 250–850） |
| 行号锚点 | **持续漂移**（`utils.js` 在 T-105 ~ T-118 间持续插改，函数位置整体下移；T-115 当时更新的两处也已被 T-118 的新增代码再次推后）。**T-119 更新**：正文引用的锚点已按当前文件修正或改为「函数名为准」的写法；§5 台账里的行号锚点保留原值仅作历史标记，**定位以函数名为准**（`allocFat` / `allocGrain` / `calcProfile` / `dailyPreview` / `evalRules` / `perMealIntake` / `potDaysOf`） |

**后续任务引起的新口径变化（登记）**：

| # | 变更 | 对本台账的影响 |
|---|---|---|
| R1 | **T-112 规则引擎重写**：判据由「7 日均线周降幅（>1.2 加主食 / <0.2 且打卡率 ≥80% 减主食）」改为「近 14 天体重变化率 × 阶段阈值（增肌 +1%~+2%、减脂 −2%~−3%）」，动作改为碳水 ±0.5 g/kg（配额模式）或每份大米 ±15 g（TDEE 模式），并新增「停止减脂」「重算配额」两条提醒 | §6.3 ④ 的旧描述**已作废并就地更新**；§7 监控清单「跑满两周再调」的结论不变。新增回归网 `verify/rules-audit.mjs`（97 项）。**不触及本台账的对账方法与三方一致性结论** |
| R2 | **T-113 食物性质**：`foods` 新增 `nature`（13 值枚举），并配 `backfillPresetNatures()` 幂等回填 | **不涉及营养计算**：`calcRecipeTotals` / `calcTotals` 的入参不变，性质只驱动今日页提示层（排除清单三类 + 生熟口径），不改克数、不拦截记录。§1 对账方法与 §2 全部数值**无需变更** |
| R3 | **T-108 / T-109 / T-111 餐次分包与小数份**：`mealAllocation` 落表、`consume` / `restore` 带 `mealSlot`、`meals` 放开为 `REAL 0–8`（0.1 网格） | §3.3 的「分叉已结构性消除」在小数份下同样成立（回撤按 `Σ portions` 对称归还）；§4.2 的「只分 2 餐」诊断仍成立但已不再无解（见该节 T-115 复核注）。`dailyPreview` 口径未变，故 §2.3 的预演数值**无需变更** |
| R4 | **T-101 的 D3 修复** | §2.1 的 `134 / 213` 与 §5 台账 D3 行**已一致**（T-110 已同步，本次复核未发现新偏差） |

> **结论**：本台账的核心结论（计算层无错、三方一致、NAS 期望值、附录断言数）**全部仍然成立**；本次实际变更只有两处 —— §6.3 ④ 的规则引擎描述（已更新）与 §4.2 的补充复核注（已加）。

> **T-119 追加（2026-09-18）**：上表的「附录断言数」已按实跑更新为 **69 项**（附录的脚本清单与锚点说明已同步更新）；§4.2 的复核注已按 T-118 的真实口径重写（原文「`dailyPreview` 口径未变」已失真）；§5.2 新增 **R6**。其余结论不变。

> **T-127 追加（2026-09-18）**：断言数再次按实跑更新（`calc-audit` **89** / `meal-slots-audit` **338** / `rules-audit` **123** / `backup-audit` **29**，附录脚本清单同步），并新增 §5.3 登记 **R7~R9**（体重反馈改双窗均值、日类型改按天登记、餐次开关与 `mealTargets` 第 5 形参）。

### 5.2 有氧模块口径登记（T-116 · 2026-09-18）

**结论：需要登记。** 理由：有氧本身不是营养计算（不参与 `calcRecipeTotals` / `calcTotals` / 配额表，也不进 TDEE 与 `gap`），但它的输出——「本周日均消耗 ÷ 100 × 25 g 碳水」——会**加到今日页的当日碳水目标上**（`store.profileWithCardio`）。也就是说它会改变"今天该吃多少"，口径错一次就是直接放大或吃掉减脂缺口，故属于必须留痕的口径变化。

| # | 变更 | 说明 |
|---|---|---|
| R5 | **有氧消耗公式由 `活动心率 ÷ 静息心率 × 6` 修正为 `活动心率 ÷ 静息心率 × 6.4 − 6.2`** | 成因：T-114 读取官方 Excel 第 16 表 B14 单元格文本时**被截断误读**，漏掉了「.4 − 6.2」，只留下一个「6」。旧口径约为表值的 2.5 倍（静息 70 / 运动 120：每 kg 每小时 **10.29 → 4.77** kcal）。同表 F 列 `= ROUND($E14 × 体重, -1)` 的「每 kg 值 × 体重 × 小时」链路不变；另加「活动心率 ≤ 静息心率 → 0」的非负钳制（公式含 −6.2 偏移，否则会出负消耗反向扣额度） |
| R6 | **T-118 分包口径回摆（T-117 的口径被推翻）**：`dailyPreview` 的日总量曾由「每份 × 2 + 加项」改为 `Σ (每份营养 × 各餐份数)`（把「一锅 = 一天」当作分包语义，用户一标分包就 +120% 爆红），T-118 推翻并恢复原口径 —— 日总量**恒为**「每份 × `MEALS_PER_DAY`(2) + 加项」，分包只按各餐比例把这 2 份拆到各餐（`perMealIntake`），一锅天数 = `⌈份数 ÷ 2⌉`（`potDaysOf`）。`Σ mealAllocation = portions` 的**数据层不变量不受影响**，被推翻的只是「拿 Σ 各餐份数当一天的量」这个推论 | §2.3 的预演数值、§4.3 的红黄绿口径**无需变更**（口径已回到原值）；§4.2 复核注已按现口径重写；`calc-audit` 新增 `[L]` 段守住「分包 / 未分包日总量逐值相等」，断言数 **44 → 69** |

**对台账的影响**：

- §1 对账方法、§2 计算链路、§3 实际执行核算、§6 加餐建议**全部不变** —— 有氧不入 TDEE、不改 `gap`、不改配额克数。
- 唯一被影响的是**今日页碳水目标的有氧加成**。量化对照（静息 70 / 运动 120 / 70 kg）：

| 场景 | 旧 ×6 口径 | 新 ×6.4−6.2 | 变化 |
|---|---|---|---|
| 每 kg 每小时 | 10.29 kcal | 4.77 kcal | −53.6% |
| 每周 2 小时（120 分钟）消耗 | 1440 kcal | 668 kcal | −53.6% |
| 摊到每天（÷7） | 206 kcal | 95 kcal | −53.9% |
| **可多吃的碳水（100 kcal ≈ 25 g）** | **51 g/天** | **24 g/天** | **−27 g/天** |

> 即：修正后「每周 2 小时有氧」能换到的碳水只有原来的一半左右。旧口径等于每天凭空多发 27 g 碳水额度（≈108 kcal），把本已设好的减脂缺口吃掉 —— 这正是本次修正的意义。
- 回归网：`verify/cardio-audit.mjs`（**68 项**，改用官方第 16 表表值精确断言、删去旧口径 WARN 段）与 `verify/backup-audit.mjs`（含 `cardio_logs` 往返）。有氧模块详情见 [SPEC.md](file:///d:/02工作/健身助手/SPEC.md) §6。

### 5.3 按天登记日类型与双窗均值判据口径登记（T-126 · 2026-09-18）

**结论：需要登记。** 本轮三项改动都直接改变「今天该吃多少」与「该不该调碳水」的结论，且都是**静默生效**的（不报错、界面上看不出异常），故必须留痕。

| # | 变更 | 说明 |
|---|---|---|
| R7 | **体重反馈判据由「窗口首末两点」改为「相邻两个 14 天窗口的体重均值」** | 旧口径（T-112）取近 14 天窗口内**首末两条**记录算变化率，单日喝水 / 熬夜造成的波动会被首末两点整体放大；新口径 W1 = 近 14 天、W0 = 紧邻其前 14 天（两窗按日期相减，不相交、不留缝），变化率 =（W1 均值 − W0 均值）÷ W0 均值。**行为变化：判定从此需要前后两窗各至少 1 条记录（约两周数据），原「近窗不足 2 条 → 数据不足 / 首末跨度 ≥ 7 天 → 观察期」的门槛已取消** —— 近窗一条都没有才不运行，前窗没有记录只给观察期。阈值表（增肌 +1%~+2%、减脂 −2%~−3%）与动作（碳水 ±0.5 g/kg 或大米 ±15 g/份）**逐值不变** |
| R8 | **日类型由「设置里的静态值」改为「按天登记」** | `day_logs.day_type` 新增（`'train'`\|`'rest'`\|`'none'`\|`NULL`），生效优先级 = **当日登记值 → `settings.dayType`（降级为「默认日类型」）→ `'none'`**，由 `utils.pickDayType` 统一给出（`store.todayDayType`）。登记只作用于登记那天；**「未登记」与「登记为 `none`」是两种语义**（前者回退设置值，后者是当天明确表态）。连带：`calcQuotaProfile` 新增可选第 3 参 `dayType`，「力训当天即按训练日档位与分餐比例吃」 |
| R9 | **餐次可关闭 + `mealTargets` 新增第 5 形参 `slotsOff`** | 新键 `settings.mealSlotsOff`（默认 `[]`，元素 ∈ 六餐全集，去重后不可全关）。关闭的餐次从当天分配结构整体移除，其碳水比例按比例归一化给其余餐次（`Σ carbRatio` = 1、各餐合计 = 全天配额）。**「零食/夜宵」在口径上就是那张「晚加餐」机动卡**，关闭 `snack` 时今日页整张卡隐藏。另新增 `settings.lastRecalcWeight` 作为「每降 5 kg 提醒重算」的基准，`evalRules` 新增动作 `recalc_quota` |

**对台账的影响**：

- §1 对账方法、§2 计算链路（BMR/TDEE/配额克数、一锅总量、每份与预演）、§3 实际执行核算、§4 偏差归因（含 §4.2 的「只分 2 餐」诊断）、§5.1 / §5.2 的历史登记**全部不变** —— 三项改动都不触碰 `calcRecipeTotals` / `calcTotals` / `calcProfile` 的入参与数值。
- 被替换的只有 §6.3 ④ 的判据描述（已就地更新），以及今日页「各餐目标 / 晚加餐」两块的展示口径。
- 回归网：新增 `verify/meal-slots-audit.mjs`（**338 项**，餐次开关 + 当日日类型优先级 + 餐次枚举同源），`rules-audit` 因双窗均值扩到 **123 项**（T-112 时为 97 项），`backup-audit` 因 `day_logs.day_type` 往返扩到 **29 项**，`calc-audit` 实跑 **89 项**。详见 [SPEC.md](file:///d:/02工作/健身助手/SPEC.md) 第 4 节验收断言。

> **T-127 复核（2026-09-18）**：本轮把上面三项收口到「三页同源」—— 配方页 / 做饭页的 `mealTargets` 此前仍读 `settings.dayType` 且不传 `mealSlotsOff`（会按静态日类型分包、并给已关闭的餐次分到份数），已改为与今日页同传 `store.todayDayType` + `settings.mealSlotsOff`；`MEAL_SLOTS` 收紧为 `helpers.js` 单点定义、settings 路由引用同一份（由 `meal-slots-audit` [F] 段守住）。

### 5.4 外食 / 喝酒的记录与随餐修正口径登记（T-129 · 2026-09-18）

**结论：需要登记。** 本轮新增的是一条**新的额度分配层**（不改 `mealTargets` 自身），它同时改变「当日摄入读数」与「各餐目标」两处结论，且只在界面上体现为几个数字 —— 不报错、看不出异常，故必须留痕。

| # | 变更 | 说明 |
|---|---|---|
| R10 | **外食/喝酒记录进 `day_logs.outing`，折算量计入当日摄入** | 字段 `{type,level,baijiu,beer,slot}`，`NULL` = 未登记。类型 ∈ `eat`/`drink`/`both`，量级 ∈ `light`/`normal`/`big`，餐次 ∈ 六餐全集。折算口径**照抄官方第 17 表问答 13**：1 两白酒（50g）≈ 1 瓶啤酒（600g）≈ **200 kcal ≈ 50g 碳水**，酒整块记作碳水、蛋白与脂肪记 0（他的执行口径是「先算酒精摄入，再从饮食里扣」）。外食按粗档位估算：轻量 375 / 普通 530 / 大餐 840 kcal（口径标为估算）。`utils.dayIntake` 增加 outing 一项，今日环 / 各餐完成度 / 记录页回溯与达标天数随之变化 |
| R11 | **新增 `utils.adjustForOuting(plan, outing)`：缺口只从「外食餐次的前后两餐」扣** | 这是用户原话「约了之后可以在外食前后两餐动态调整修正」的落地层，**叠加在 `mealTargets` 之上**（后者的签名与返回结构一字未动）。算法：对外食折算量的每个宏量（顺序 `c → p → f`，即**碳水优先扣**），在锚点餐次的**前一餐 + 后一餐**之间按各自该类目标的占比分摊，每餐扣到 0 即停、绝不出现负数。**硬不变量：前后两餐扣得动时 `Σ 各餐 = 全天配额 − 外食占用` 严格成立**；扣不动时差额（记作 `over`）必须在界面上明示「今天已超 X」，**不得静默显示绿色**。锚点不在当天结构里（登记后改了日类型 / 关掉了那一餐）时退回「当天最后两餐」，宁可扣得保守也不静默不扣 |
| R12 | **两处界面口径收口** | ① 设置页「训练时间点」的禁用条件由 `settings.dayType !== 'train'` 改为 `todayDayType !== 'train'`（T-126 起日类型是按天登记的，只判静态值会让「当天登记为训练日、默认日类型不是训练日」的用户改不了练前餐序 —— 纯界面失配，引擎计算本来就不受影响）；② 关闭「零食/夜宵」后，历史已记录的晚加餐条目**继续计入**当日摄入（真实吃过的不抹掉），但界面必须明说，不再"卡隐藏了数字还在"地静默不一致 |

**对台账的影响**：

- §1 对账方法、§2 计算链路（BMR/TDEE/配额克数、一锅总量、每份与预演）、§3 实际执行核算、§4 偏差归因、§5.1 ~ §5.3 的历史登记**全部不变**：本轮不改 `calcProfile` / `calcQuotaProfile` / `mealTargets` / `calcTotals` 的任何入参与数值，`adjustForOuting` 只在「当日已登记外食」时才改变各餐目标（未登记时逐字段等价于原结构）。
- 新增回归网：`verify/outing-audit.mjs`（**100 项**，折算口径 + 归一化脏输入 + 守恒不变量 + 已超边界 + 删除回落 + 前后端枚举/迁移/备份白名单的源码级一致性）；`backup-audit` 因 `day_logs.outing` 往返扩到 **30 项**。其余七个脚本项数不变（89 / 175 / 453 / 338 / 123 / 68 / 65）。

---

## 6. 关于「人不自觉多摄入 20%」

### 6.1 理论本身成立

自我报告的能量摄入与**双标水法（DLW，金标准）**对照研究一致显示：食物记录/24h 回顾普遍存在低报，幅度常见 **20–30%**，且 BMI 越高低报越明显（Archer, Hand & Blair, *PLoS ONE* 2013 对 NHANES 的分析即指出自我报告摄入在生理上不可信）。机制是**漏记加餐零食、低估份量、社交场合不记**。

### 6.2 但不能把 20% 直接扣在目标热量上

这是**两个不同层面的量**，叠加会双重扣减：

| 层面 | 含义 | 本系统对应 |
|---|---|---|
| 测量误差 | 记录值 vs 真实摄入 | 本系统用**称重清单**记录，已属降低低报的手段 |
| 能量目标 | 应该吃多少 | `目标 = TDEE − gap`，已含 750 kcal 缺口（29.4% TDEE） |

若目标再砍 20%：1799 → 1439 kcal，缺口变为 1110 kcal（**43.5% TDEE**），已进入极低热量饮食（VLCD）区间，代价是肌肉流失、代谢适应、瘦素下降与暴食反弹。**且你的 09-17 数据已证明：真正吃掉热量的是加餐，不是目标设高了。**

### 6.3 正确的「预留 20%」做法

你直觉里对的部分是：**应该给不可控的加餐留出预算**。做法不是砍目标，而是**调整正餐与加餐的份额分配**：

**① 正餐降 15–20%（减油/减蛋/减玉米，米饭不动）**

| 食材 | 现在 | 建议 | 每份热量变化 |
|---|---|---|---|
| 食用油 | 60 g | 40 g | −20 kcal |
| 鸡蛋 | 600 g（12 个） | 400 g（8 个） | −48 kcal |
| 玉米粒 | 540 g | 400 g | −26 kcal |
| 大米 | 600 g | **保持** | 0（饭量是饱腹主力，且能量密度最低） |
| **每份合计** | 805 kcal | **约 711 kcal** | **−94 kcal（−11.7%）** |

**② 把腾出的份额变成显式的「加餐预算」**

```
目标 1799 kcal
├── 正餐 2 份        1422 kcal  ← 降到 711/份
├── 蛋白粉 2 勺       230 kcal  ← 固定，天天补
└── 机动加餐预算      147 kcal  ← 只有真的饿才动
```

**③ 加餐分级（你的「饿了再补」思路，加上优先级）**

| 状态 | 动作 | 代价 |
|---|---|---|
| 不饿 | 什么都不加 | 全天 1652 kcal，缺口 897 |
| 有点饿 | 蛋白粉 1 勺 | +115 kcal |
| 明显饿 | 红薯 150–200 g（或 1 个鸡蛋） | +92~122 kcal |
| 还饿 | 西兰花 / 黄瓜等叶菜，几乎不占热量 | ≈0 |

> **鸡蛋要单独管住**：6 个蛋 = 433 kcal + 26.5 g 脂肪，是 09-17 爆表的元凶；1–2 个足够。

**④ 用体重趋势校准，而不是拍脑袋**

已有 [evalRules](file:///d:/02工作/健身助手/frontend/src/utils.js) 规则引擎（**T-112 已重写，本行原「7 日均线周降幅」口径作废；T-126 再改判定口径，见下**）：判定窗口固定 **2 周（14 天）**，**T-126 起取「相邻两个 14 天窗口的体重均值」相减算变化率**（W1 = 近 14 天、W0 = 紧邻其前 14 天；原「取窗口首末两条」的两点口径作废），与该阶段阈值比对 —— 增肌 **+1%~+2%**、减脂 **−2%~−3%** 为「不调整区间」，超出/不足才给动作；**判定从此需要前后两窗各至少 1 条记录（约两周数据），原「首末跨度 ≥ 7 天 → 观察期」的门槛已取消**（近窗一条都没有 → 不运行；前窗没有 → 观察期）。动作按 `calcMode` 分流：配额模式调 **碳水 ±0.5 g/kg**，TDEE 模式调 **每份大米干重 ±15 g**（两者都只动碳水，蛋白与脂肪不动）。另有「停止减脂」（BMI 男 ≤23 / 女 ≤21，或腰围男 >85cm / 女 >80cm）与「重算配额」两条提醒 —— 后者基准优先取 `settings.lastRecalcWeight`（T-126 新增，一键应用 `recalc_quota` 写回），缺省时退回首条记录。**跑满两周再调，比任何预设比例都准。**

---

## 7. 日常监控清单

每次改配方或改计算层代码后，逐项核对：

- [ ] 一锅总量：`合计热量 = Σ(每100g值 × 克重 ÷ 100)`，与页面显示一致
- [ ] 每份 = 一锅总量 ÷ 份数，且 **份数 = 天数 × 2**
- [ ] 每日预演（2 份 + 加项）四项偏差 ≤ 10%（绿）
- [x] ~~当天 `consumed` 必须等于 `meals`~~ —— v2.4 起由「一次性打卡」结构保证，不再需要人工核对
- [ ] 加餐是否已计入当日，而不是游离在预演之外
- [ ] 称重口径：主食按干重、鸡蛋去壳、虾仁去冰衣
- [ ] 库存为空时，`perSnap` 与当前配方每份是否一致

---

## 附录 · 验算脚本

```powershell
# 在项目根目录执行（九个脚本一起跑，全绿退出码 0；项数为 T-129 实跑值）
node verify/calc-audit.mjs         # 89 项：TDEE 派 + 三方对账（本文主体，含 [L] 分包口径段）
node verify/quota-audit.mjs        # 175 项：配额派（配额表 / BMI 修正 / 覆盖值）
node verify/meal-audit.mjs         # 453 项：分餐引擎（七档餐序 + 契约 §3.3/§3.4/§3.5 算例）
node verify/meal-slots-audit.mjs   # 338 项：餐次开关 + 当日日类型优先级 + 枚举同源（T-126/T-127，见 §5.3）
node verify/rules-audit.mjs        # 123 项：规则引擎（双窗均值判据 / 阈值 / 动作 / 提醒）
node verify/cardio-audit.mjs       # 68 项：有氧消耗与饮食置换（T-116 按官方表值口径，见 §5.2）
node verify/backup-audit.mjs       # 30 项：备份导出/导入往返（含 cardio_logs 可空心率、day_logs.day_type / outing）
node verify/food-fields-audit.mjs  # 65 项：foods.gi / cookedWeight 字段（T-120）
node verify/outing-audit.mjs       # 100 项：外食/喝酒折算与随餐修正（T-129，见 §5.4）
```

`calc-audit` 直接 import 前端计算层（`frontend/src/utils.js` 是纯计算、不依赖 Vue），对参数链、一锅总量、每份、每日预演、每餐目标、微调补偿、分包口径做数值断言，并附加 D1/D2 两项缺陷的复现检查（修复后应输出「未复现」）。最近一次运行结果：**89 项通过 / 0 项失败**（退出码 0）。

> T-119 复审（2026-09-18）时六个脚本为 69 / 164 / 323 / 97 / 68 / 26 项通过、0 项失败。**T-127 实跑（2026-09-18）八个脚本分别为 89 / 175 / 453 / 338 / 123 / 68 / 29 / 46 项通过、0 项失败**，本文附录与 §5.1 / §5.3 的断言数已按实跑更新。

实测输出（`[I]` 段）复现出当前配方的自动搭配原型：

- items：mushroom 300g / onion 300g / carrot 300g / rice 360g / corn 900g / egg 600g / shrimp 900g / oil 60g
- 每份：734 kcal · P 41.49 · C 96.44 · F 22.47 · 生重 620g
- 每餐目标：781.1 kcal · P 43.5 · C 102.9 · F 21.5

对比线上配方（`rice 600 / corn 540`）可见：**米饭被手工从 360 g 加到 600 g（+67%），玉米从 900 g 减到 540 g**。米饭是能量密度最低、饱腹感最强的部分，加量符合「要吃饱」的直觉，但也把每份从 734 kcal 抬到 805 kcal。
