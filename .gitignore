# 依赖
node_modules/

# 构建产物
frontend/dist/
backend/public/

# 本地数据库（Docker 内数据走卷挂载，不入库）
data/
*.db
*.db-wal
*.db-shm

# 环境与编辑器
.env
.vscode/
.idea/

# 浏览器自动化运行时产物
.playwright-cli/

# 发版 Skill 的 NAS 连接配置（含主机/账号等基础设施信息，仅本地保留）
.trae/skills/release-deploy/nas-config.json

# 外部资料与调研中间产物：体积过大（Excel 套表 52.8MB；音频 128MB + ffmpeg 86MB），
# 且属可回溯的外部来源，不入库。文字成品（逐字稿 .md）保留可入库。
# 注意：用 `**` 递归匹配整棵子树 —— 只写 `目录/*.jpg` 漏掉了 frames/ 这类子目录（踩过）。
【可任意分享】健身Excel超级套表（作者：B站好人松松）/
好人松松调研/**
!好人松松调研/*.md
